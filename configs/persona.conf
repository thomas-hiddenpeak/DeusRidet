# ============================================================================
# DeusRidet — Persona Configuration
# ============================================================================

# Entity identity
name = 黑娃

# Aliases / wake words — hearing these boosts wakefulness (cocktail party effect)
# The entity does NOT automatically respond; it raises attention, then
# a probe decode lets the MODEL decide whether to speak or stay silent.
aliases = 黑娃儿,Heiwa,heiwá

# ============================================================================
# Decode behavior
# ============================================================================

# Max tokens for speech output (short, concise responses)
speech_max_tokens = 80

# Max tokens for thinking (internal analysis)
thinking_max_tokens = 256

# Check input queue every N decode tokens (interleave granularity)
decode_interleave_tokens = 4

# ============================================================================
# Personality (future use by Persona module)
# ============================================================================
# personality_traits = curious,direct,witty
# speech_style = concise
# emotional_baseline = calm
