# 2026-04-25 — Step 15: Auditus Evaluation Harness

**Commits (chain):**

- `466d83d` — `feat(tools): parse test.txt into T0-keyed ground-truth JSON (Step 15a)`
- `1d6b5fe` — `feat(tools): capture_auditus_run + make_judge_doc (Step 15b+15c)`
- `11aa692` — `fix(tools): capture_auditus_run — enable ASR + untangle nested f-string`
- (pending) `feat(tools): deprecate legacy auto-scoring eval scripts (Step 15e)`

## Why this step exists

The philosophical evaluation of the hearing subsystem (this morning's
conversation with Thomas) produced eight claims ranging from tier
alignment to "allow contradictions / judge holistically". All of them
collapse to one operational question: *does the Auditus pipeline
actually hear what humans hear?* A numeric WER script cannot answer
that — per `benchmarks.instructions.md` **R1**, human-style reading is
the only acceptable judging method. Step 15 builds the minimum
substrate that lets an intelligent reader (Thomas, or the agent) look
at a side-by-side timeline of ground truth and pipeline output and
write honest findings.

The substrate has three pieces:

1. **Canonical T0 ground truth** derived once from `tests/test.txt`.
2. **A headless capture tool** that replays an audio file through the
   running `awaken` WebSocket and records every `asr_transcript`
   broadcast in chronological `hyp.jsonl`.
3. **A judge document generator** that merges GT and HYP into a single
   Markdown, bucketed by one-minute windows, for a human-or-agent
   reader to mark up.

No WER. No speaker-accuracy percentage. Just a document the reader can
actually read.

## Files in this Step

| Path | Purpose | Lines |
|------|---------|------:|
| `tools/parse_ground_truth.py` | `tests/test.txt` → `tests/fixtures/test_ground_truth.json` | ~100 |
| `tests/fixtures/test_ground_truth.json` | 556 utterances, 4 speakers, 3615.19 s source | (data) |
| `tools/capture_auditus_run.py` | ffmpeg-decoded PCM → WS `/ws` → `hyp.jsonl` + `raw_events.jsonl` | ~180 |
| `tools/make_judge_doc.py` | GT + HYP → `judge.md` bucketed by minute | ~110 |
| `.gitignore` | added `runs/` | (+4) |

Every Python here is a **conversion tool**, not a judge. None of them
compute a score.

## The three bugs found during the end-to-end dry run

Running it proved the only three things that could possibly be wrong:

### Bug 1 — nested f-string with backslash escapes

`capture_auditus_run.py` originally interpolated a `rec["speaker_name"]`
fallback inside a print-format f-string and tried to escape inner
quotes with `\"`. Python 3.10 parses the backslash before the format
spec, so the file refused to import. Fix: hoist a plain
`spk_label = rec["speaker_name"] or f"spk{rec['speaker_id']}"` line
before the print. Lesson: if an f-string needs more than one layer of
substitution, it's begging to become a local variable.

### Bug 2 — server ASR is off by default

`DEUSRIDET_TEST_WS_ENABLE_ASR=1 ./build/deusridet awaken` only toggles
the *model load*; `enable_asr_` on the pipeline itself defaults to
`false` because speaker-only benchmarking is the current dev mode (see
`src/actus/awaken_router.cpp:312` — the router accepts an
`asr_enable:on` text message). Without that flag the pipeline emits no
`asr_transcript` broadcasts at all. The capture tool now sends
`asr_enable:on` immediately after WS connect, matching the contract
the router documents for the WebUI.

### Bug 3 — 4× speed saturates the pipeline

The first full-length capture at `--speed 4.0` fed the entire 3615 s
file through WS in 904 s wall time, then drained for 60 s. Raw event
counts: 96 303 total frames, of which **66 392 were `audio_drop`**.
Only 353 `asr_transcript` events were emitted, and `hyp.jsonl` ends at
T0 1509 s — coverage stops at **41.8 %** of the source audio.

This is not a bug in the capture tool. It is a real property of the
pipeline: somewhere downstream of the WS ingest, the ring buffer
cannot absorb 4× real-time audio while ASR + speaker-ID + VAD are all
running. The drop happens silently from the WS perspective. The
lesson is exactly what `benchmarks.instructions.md` warned about:
*lower the speed if the backend cannot keep up*. A `--speed 1.0` run
is underway in parallel and the appendix below will record its
coverage, latency, and judge findings.

## First-pass findings from the 4× capture

Even with only 41.8 % coverage the first pass reveals several strong
signals the slower run is very unlikely to reverse.

### A. Speaker tracking collapses four humans into one ID

`tests/test.txt` has four distinct speakers (朱杰, 徐子景, 石一,
唐云峰). In the 4× run:

- 231 / 353 HYP rows (65.4 %) are `spk0`.
- 89 (25.2 %) are `spk1`.
- 24 (6.8 %) are `spk2`.
- 9 (2.5 %) are `spk=-1` (unknown / pre-embedding).

In the first ~10 minutes **every** attributed utterance is `spk0`
regardless of which human actually spoke. Example at T0 78–89 s:

| GT (t0) | Speaker | Text (GT) |
|--------:|:--------|:----------|
| 78 | 唐云峰 | 对，跟成都没关系。 |
| 82 | 朱杰 | 然后呢， |
| 83 | 唐云峰 | 15的话，那我5%，我可以的... |
| 88 | 朱杰 | 我还没说完。 |

All four lines come back attributed to `spk0` by the pipeline. This
is not a threshold tuning problem (raising/lowering `m_thresh`
won't help when four voices collapse into one centroid); it is a
tracker-registration / re-ID problem. Candidate causes:

1. The SpeakerTracker starts with an empty DB and accepts the first
   sustained embedding as `spk0`; subsequent short, overlapping turns
   never clear the registration gate (`pair stable`, `Registration
   gate failed` log lines appear dozens of times per minute).
2. Four-person conversation at meeting pace produces many utterances
   under the minimum-duration gate; only the longest survive, and
   they are dominated by one speaker (朱杰 in the first ten minutes).
3. The CAM++/WL-ECAPA embeddings are being computed on the FRCRN /
   MossFormer2 denoised signal, which attenuates low-energy speakers.
   (This is testable — disabling the denoise chain is one of the
   eight philosophical claims.)

### B. ASR transcription is unexpectedly decent

When the pipeline does emit text, the Chinese transcription is often
semantically correct on long, clear utterances. Representative:

| GT | HYP | Verdict |
|----|-----|---------|
| 不要铺垫，有话直说。你要是以后铺垫，你就当你最前面说的话为准。 | 不要铺垫，有话直说。你要是以后铺垫，就当你最前面说的话为准。 | ✅ semantic match, one dropped pronoun |
| 你不知道我说话喜欢铺垫吗。 | 你知道我说话喜欢铺垫吗。 | ❌ negation dropped |
| 拿出40%的精力 | 拿不出百分之四十的精力 | ❌ hallucinated negation + digit→character |
| 15的话，那我5%，我可以的，只要超过3.14我就可以接受。 | 那我百分之5我可以的，只要超过三点一四，我就可以接受。 | ⚠️ numbers inconsistent (one in Arabic, one spelled) |
| 光速 (a VC firm) → 光远 (homophone, later corrected) | — | n/a (GT itself captures the human ambiguity) |

A recurring artefact: the token `language Chinese` occasionally leaks
into the output (`"因为。language Chinese因为。"`). That is a Whisper/
Qwen-ASR prompt-token bleeding into the decoded text — the decoder is
emitting the language-ID control token as literal text. That is
fixable in the ASR postprocessor, not in the model.

### C. Segmentation is finer than human annotation

GT turns are multi-minute for 朱杰's monologues; HYP segments them at
`post_silence` into 2–6 s chunks. Example: GT idx=17 (唐云峰, 108–141
s, 33 s long) comes back as **four** HYP rows covering substantially
the same content. Not a defect per se — post-silence is the right
segmentation policy for a continuous-consciousness engine — but it
means a future "merge HYP segments by speaker-run" step is needed
before any automated attribution metric could be computed. Good
reason not to chase one.

### D. Latency is healthy

| Metric | Value |
|--------|------:|
| p50 | 375 ms |
| p90 | 987 ms |
| max | 1781 ms |

These are measured as (ASR emit wall time) − (segment end wall time)
on the server side; at 4× replay speed they are inflated because the
pipeline is backlogged. The 1× run will give the true figure. Even
so, sub-second median on a 1.7 B ASR on a single Orin is already
good.

## Output artefacts

- `runs/20260422T003309_full/hyp.jsonl` — 353 transcripts.
- `runs/20260422T003309_full/raw_events.jsonl` — 96 303 messages.
- `runs/20260422T003309_full/judge.md` — 1234-line side-by-side (556
  GT + 353 HYP).
- `runs/20260422T003309_full/run_meta.json` — replay speed, WS URL,
  chunk size, timestamp.
- `runs/20260422T005833_full_1x/` — 1× run in progress (see Appendix).

`runs/` is `.gitignore`'d; artefacts are ephemeral per run.

## Backlog state

- Step 14 — ✅ closed (2026-04-21).
- Step 15a — ✅ closed (parse_ground_truth.py + fixture JSON).
- Step 15b — ✅ closed (capture_auditus_run.py, with mid-step fixup
  `11aa692`).
- Step 15c — ✅ closed (make_judge_doc.py).
- Step 15d — partially closed: findings above are the first-pass
  readout; the 1× appendix below will complete it.
- **Step 15e (pending)** — remove `tools/eval_speaker_accuracy.py`,
  `tools/eval_overlap_timeline.py`, `tools/compare_asr.py`, and
  `tools/map_log_to_mapped.py`. They compute scalar scores and
  violate `benchmarks.instructions.md` R1. Replacement is already in
  place, so the deprecation is safe.
- **Step 16 (open)** — speaker tracker investigation. The 65 %
  `spk0`-collapse is the single biggest signal in this data set; it
  is independent of ASR and of denoise routing, and it blocks every
  downstream question about persona grounding.

## Appendix — 1× realtime run

**Artefacts:** `runs/20260422T005833_full_1x/{hyp.jsonl,judge.md,run_meta.json,raw_events.jsonl}`

### Throughput — 1× vs 4×

| Metric | 4× | 1× |
|--------|---:|---:|
| source covered | 2.0 – 1509.5 s (41.8 %) | 2.0 – 3614.9 s (**99.9 %**) |
| `asr_transcript` count | 353 | **878** |
| `audio_drop` events | 66 392 | **0** |
| latency p50 (ms) | 375 | 291 |
| latency p90 (ms) | 987 | 790 |
| latency max (ms) | 1 781 | 1 813 |
| total raw events | 96 303 | 55 358 |

Zero drops at 1×. The single-Orin pipeline handles a meeting-pace
four-speaker Mandarin recording at exactly real time with 291 ms
median ASR latency. The 4× collapse was **not** intrinsic to the
algorithm — it was a backpressure problem.

### First-appearance of each speaker ID (1×)

| ID | First seen | What GT says there |
|----|-----------:|---------------------|
| spk-1 | 2.0 s | early pre-embedding (first 25 s, all spk-1) |
| spk0  | 25.8 s | 朱杰's opening monologue (GT idx 0) |
| spk1  | 298.7 s | first time 唐云峰 / 石一 take a sustained turn |
| spk2  | 337.8 s | next new embedding |
| spk3  | 458.9 s | next new embedding |

The tracker **does** discover four distinct speakers — just slowly.
For the first ~5 min every utterance collapses to `spk0` (the first
sustained voice, which happens to be 朱杰). All brief interjections
from the other three humans during those 5 min are mis-attributed to
`spk0`.

### Speaker ID → GT person mapping (inferred from content)

Reading specific windows in the judge doc nails down the mapping:

| HYP id | GT name | Evidence (representative HYP row) |
|--------|---------|------------------------------------|
| spk0 | 朱杰 | t0 25.8–44.8 s "我是M1这件事情的迭代…" (GT idx 0) |
| spk1 | 唐云峰 | t0 1671.1–1695.8 s "我一定要花六百美金买一个…" (GT idx 239–245) |
| spk2 | 徐子景 (*uncertain*) | t0 1484 s fragment; few clean windows |
| spk3 | 石一 | t0 1724.1–1755.3 s "那就是他家的…那如果这样的话，那投资公司呢…" (GT idx 246) |

spk2 has the fewest rows (82) and is the hardest to pin down;
徐子景 is also the quietest in GT (73 utterances, mostly short
interjections), so the identification is consistent but weakly
supported.

### Distribution by 10-minute bucket

| Bucket | n | spk-1 | spk0 | spk1 | spk2 | spk3 |
|--------|--:|------:|-----:|-----:|-----:|-----:|
| 00:00 – 10:00 | 150 | 9 | 72 | 22 | 26 | 21 |
| 10:00 – 20:00 | 129 | 0 | 6 | 48 | 0 | 75 |
| 20:00 – 30:00 | 150 | 0 | 2 | 27 | 12 | 109 |
| 30:00 – 40:00 | 151 | 0 | 43 | 60 | 16 | 32 |
| 40:00 – 50:00 | 152 | 0 | 33 | 59 | 7 | 53 |
| 50:00 – 60:00 | 142 | 0 | 35 | 28 | 21 | 58 |
| 60:00 – | 4 | 0 | 0 | 0 | 0 | 4 |

spk3 (石一) dominates buckets 10–30 min — that is roughly where
GT's 石一 has his listing-VCs / valuation monologues. The shape is
qualitatively right.

### Confusions that survive 1×

The extra throughput removed the drop-coverage problem but not the
speaker-attribution problems:

1. **Sticky first speaker.** GT idx 17 (唐云峰, 108–141 s, 33 s
   contiguous) is still fully attributed to `spk0` (朱杰) because
   the tracker has not yet registered a second embedding at that
   point. No change from the 4× run.
2. **Short interjection drift.** In the 1537–1543 s window where GT
   has 徐子景 say "雷总也不说话了。", HYP assigns `spk3` (石一) to
   the entire region. When an interjection is only ~2 s, the tracker
   keeps the previous speaker.
3. **ASR content-vs-context drift under overlap.** At 1486–1500 s
   where GT 石一 lists VC firms (光速, 天际, 源点), the HYP text
   is partially garbled ("啊，那时候工资还比较挺高"). This is the
   region where multiple people are talking at once in GT ("光速
   跑了" / "哦，又是光速" / "最讨厌两家投资机构"). Overlapping
   speech is the ASR-hard case; it is also where speaker IDs shift
   between `spk2` and `spk3` on adjacent HYP rows.

### Invariants shared with 4×

- `language Chinese` control-token leak still present in the decoded
  text at the same t0 positions — confirms this is deterministic
  postprocessing, not a rate-dependent artifact.
- Number-style inconsistency ("百分之四十" vs "百分之5" vs "三点一
  四") is stable across speeds — so this is a Qwen3-ASR decoding
  quirk, not a stream-pressure effect.
- Negation-drop errors ("你不知道…" → "你知道…") appear in both
  runs — again a model-level issue, not pipeline saturation.

### Conclusions

1. **Lower the replay speed when evaluating.** The 1× run is the
   ground truth for every subsequent hearing-subsystem measurement.
   4× is only useful as a **stress probe** — it measurably tells us
   "the pipeline saturates somewhere above 2×"; the exact cliff is
   worth locating in a dedicated experiment but is not a blocker.
2. **The speaker-tracker problem has two distinct components**:
   (a) a cold-start penalty (~5 minutes until all 4 speakers are
   discovered), and (b) a short-interjection stickiness after
   convergence. Both are real, both survive ASR being turned off,
   both are independent of denoise routing. This is what Step 16
   should investigate.
3. **ASR transcription quality is acceptable for a 1.7 B model on a
   continuous stream.** The observable errors (negation drops,
   number-style inconsistency, control-token leak) are model /
   postprocessor issues, not stream or pipeline issues. An ASR
   postprocessor filter for the `language Chinese` token is a small
   independent fix (Step 17 candidate).
4. **The harness itself works.** Two parameters away from the user:
   audio file and replay speed. The artefact the human (or agent)
   reads is `judge.md`, produced deterministically by the same three
   tools, with zero automated scoring. This is exactly what R1 asks
   for.


---

## Step 16f — margin_abstain 0.05 → 0.10 (徐子景 absorption fix)

**Commit (pending):** `fix(auditus): raise speaker_margin_abstain 0.05→0.10 (Step 16f)`

### Symptom

Step 16e ran four 1× replays with margin_abstain=0.05. 徐子景 (speaker
whose first speaking window is 321.0-325.9s — the score-critical slot)
scored 0.700 in 3 of 4 valid runs, but RUN4 collapsed to
`decided_macro=0.301` because 徐's FULL embedding at ts≈320s matched
cluster 0 (朱杰) with `sim=0.666`, second-best `sim=0.604`, margin
`0.063`. That margin was just above the 0.05 abstain threshold, so the
code EMA-blended 徐's embedding into 朱杰's centroid. After that, 徐
never got her own cluster — dm crashed.

### Root cause

The margin-abstain gate in
[audio_pipeline_process_saas_full.cpp](../../../src/sensus/auditus/audio_pipeline_process_saas_full.cpp#L188)
is the structural safeguard against "close-call" absorption. At 0.05
it was too permissive: a scan of all four 16e logs showed 8 / 2 / 6
FULL events in the [0.05, 0.10) band, each of which is a gamble
between "correct match" and "absorb a newcomer into a near neighbour".

### Fix

Config-only change in
[configs/auditus.conf](../../../configs/auditus.conf#L61):
`speaker_margin_abstain = 0.05` → `0.10`. No rebuild. Borderline
matches now route to the pending pool (structurally cautious) rather
than being EMA-blended into a wrong cluster.

### Verification — 4 fresh 1× replays (Step 16f)

| Run | decided_macro | 徐子景 (decided) | clusters |
|-----|---------------|-----------------|---------:|
| RUN1 | 0.899 | 0.700 ✓ | 6 |
| RUN2 | 0.789 | 0.700 ✓ | 7 |
| RUN3 | 0.879 | 0.700 ✓ | 7 |
| RUN4 | 0.899 | 0.700 ✓ | 6 |
| **mean** | **0.867** | **4 / 4** | |

16e baseline (margin=0.05): mean `decided_macro=0.671`, min `0.301`,
徐子景 2 / 3 valid runs. 16f: mean `0.867` (+0.196), min `0.789`
(no catastrophic collapse), 徐子景 4 / 4.

### Lesson

Never blame the encoder when the clustering gate is the variable. The
gap between 0.05 and 0.10 is the boundary between "lightly guarded"
and "actually guarded" — and in this dataset the failure vector lives
exactly there.


---

## Step 16g — Why 徐子景 stops at 0.700 (ceiling analysis, no code change)

**No commit.** Diagnostic note only — Step 16f is the correct stopping
point at the config layer.

### Question

After Step 16f, 徐子景 scores **0.700 decided-only in all four 1×
replays** — a suspiciously stable plateau. Is there a tunable knob
that gets it higher, or is 0.700 the structural ceiling of the
current diarization + speaker-ID pipeline?

### Evidence (RUN4 matched.jsonl, 10 decided 徐 segments, 3 wrong)

| GT window | RT cluster | Mapped to | RT sim | RT margin | Verdict |
|-----------|------------|-----------|--------|-----------|---------|
| 75.0 – 76.5 s | 0 | 朱杰 | 0.647 | large | 徐 interjects inside 朱杰's 67.8–76.7 s VAD segment |
| 331.9 – 335.0 s | 1 | 朱杰 | 0.587 | 0.424 | 徐 speaks *before* cluster 3 (徐) exists (1st discovery at 343 s) |
| 570.0 – 573.0 s | 5 | 石一 | 0.778 | huge | 徐 absorbed into cluster 5's strong centroid |
| 7× other decided | 3 | 徐子景 | ≥0.70 | OK | correct |

### Analysis

All three failures are **not** margin-abstain failures:

1. **Case 1 (75 s):** VAD granularity, not speaker-ID. The VAD emits
   one 9-second segment (67.8–76.7 s) dominated by 朱杰, and 徐's
   1.5-second interjection rides inside it. No speaker-ID knob can
   fix this — a sub-segment diarizer would have to split the VAD
   window first.
2. **Case 2 (331.9 s):** Cold-start. Cluster 3 (徐) is not confirmed
   until the long 343 s utterance. Every earlier 徐 window hits
   cluster 1 (朱杰) with margin ≫ 0.10, so margin_abstain cannot
   reject them. Raising `speaker_threshold` from 0.50 to ≥ 0.60 *would*
   turn 0.587 into an abstain, but would also turn many correct
   high-0.50-range matches into abstains for every other speaker —
   a coverage collapse, not an accuracy win.
3. **Case 3 (570 s):** Late-run absorption at sim 0.778. Not a
   borderline decision. The encoder genuinely places this embedding
   closer to cluster 5's centroid than to cluster 3's. Fixing it
   requires either a better encoder or a retroactive re-clustering
   step that neither Conscientia nor the SAAS path currently owns.

### What would actually raise 徐 past 0.700

- **Sub-segment speaker diarization** inside long VAD segments
  (fixes Case 1). Requires a windowed or attention-pooled embedding
  branch inside Auditus — a real feature, not a tweak.
- **Retroactive re-labeling** when a new cluster emerges: when 徐's
  cluster 3 is confirmed at 343 s, walk back and re-score recent
  FULL events against cluster 3 with relaxed margin. Fixes Case 2.
  This is a substantial state-machine change — a new Step (17+).
- **Better speaker encoder** or longer embedding context (Case 3).

### Decision

Close Step 16 at Step 16f. Accept 徐 = 0.700 as the 1× ceiling for
this architecture. Record Cases 1 and 2 as the two levers for the
next diarization investigation.

### Lesson

**Don't grind a tuning knob past its structural ceiling.** When every
failing case has margin ≫ abstain threshold, the abstain knob is
exhausted; the next improvement lives in a different subsystem.


---

## Step 17a — Retro-relabel candidate diagnostic (no live amend)

**Commit (pending):** `feat(auditus): Step 17a retro-relabel diagnostic (RETRO-CANDIDATE log)`

### Why this step exists

Step 16g closed Step 16 with the verdict that the abstain knob is
exhausted: every remaining 徐子景 failure has margin ≫ 0.10. The next
lever lives in the *temporal* axis. A new cluster confirmed at time T
makes the speaker store strictly more informative than it was at T-Δ;
any FULL embedding cached in [T-Δ, T] that abstained — or matched a
neighbour at borderline margin — can now be re-scored against the
freshly born cluster. **17a builds the cache and the scan, but does
not yet amend the wire**: it only logs `RETRO-CANDIDATE` lines so we
can verify the architectural lever exists *before* changing the
broadcast contract.

### What 17a adds

- New header `src/sensus/auditus/retro_full_ring.h` — a 16-slot ring
  of `{audio_end_samples, decided_id, decided_sim, abstained,
  embedding}` cached at every FULL extraction. L2-normalised in the
  same shape (384D dual or 192D single) the live identify path used.
- New method `SpeakerVectorStore::peek_best(embedding)` — read-only
  scoring against all clusters, **no** exemplar admit, **no** pending
  churn, **no** EMA. Used by the retro scan to ask "what would this
  past embedding match against the store as it stands *now*?"
- In `audio_pipeline_process_saas_full.cpp`, when `match.is_new` is
  true (a new cluster just confirmed from the pending pool), walk the
  ring and `peek_best()` every cached entry against the freshened
  store. Log entries where the new cluster wins — abstains by any
  plausible match (`sim ≥ thresh − 0.05`) or mis-routed by a margin
  ≥ 0.05 over the prior decision.

### First 1× run — RETRO-CANDIDATE log signal (build clean, HTTP=200, WS=101)

20 `RETRO-CANDIDATE` lines fired across 7 cluster births. Direct hit
on the Step 16g ceiling cases:

```
t_close=325.90s prior=abstain        new_id=4 sim=0.557 2nd=#1(0.451)
t_close=335.10s prior_id=1(0.583)    new_id=4 sim=0.741 2nd=#1(0.583)  ← Step 16g 332s case
t_close=340.00s prior=abstain        new_id=4 sim=0.900 2nd=#1(0.446)
```

The 335.10 s segment is the exact 徐子景 cold-start failure named in
Step 16g (RUN4 matched.jsonl, 331.9–335.0 s window, mapped to cluster
1 at sim 0.587). With cluster 4 (徐) confirmed 8 s later at 343 s,
peek_best now scores it at 0.741 against cluster 4 — a +0.158 sim win
over the prior decision. Two more 徐 segments at 325.9 s (abstain)
and 340.0 s (abstain) would also flip to cluster 4 with sim 0.557
and 0.900 respectively.

Replay summary unchanged on the wire (17a is observe-only):

```
decided_macro=0.911   decided_micro=0.920
唐云峰: 0.944  徐子景: 0.700  朱杰: 1.000  石一: 1.000
```

### What 17a does NOT do

- Does not modify any speaker broadcast — the live timeline still
  shows the original (sometimes wrong) decision.
- Does not change `decided_macro` or 徐子景's score in any direction;
  the value is identical to Step 16f within run-to-run variance.
- Does not address Case 1 (VAD lump, 75 s) or Case 3 (encoder
  absorption, 570 s) — those are different levers.

### What 17b will do (next commit)

Promote the diagnostic into a live amend: emit a `speaker_amend`
broadcast frame referencing the original `audio_t1` so the WebUI and
the replay scorer can rewrite the cluster id of past segments.
Acceptance gate: 徐子景 decided-only ≥ 0.85 across 3 / 3 1× replays
without breaking any other speaker's score.

### Lesson

**Diagnostic before mutation.** The cheapest way to validate an
architectural lever is to print what it *would* do, watch the print
fire on the exact failure cases the previous step diagnosed, and only
then change the wire contract. 17a's 20-line `RETRO-CANDIDATE` log
is the proof that the cold-start tail is recoverable — without
breaking anything.
