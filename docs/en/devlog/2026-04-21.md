# DEVLOG — 2026-04-21

## Step 7 Complete: Auditus Facade + Actus WS Routing

### Context

`src/actus/cmd_test_ws.cpp` had been the long-standing R1 violation —
1543 lines wiring AudioPipeline, WsServer, Orator speaker stores,
Persona, Memoria (CacheManager), and ConscientiStream into one
continuous-consciousness test harness. Step 7 of the refactor backlog
finally closes it, in five atomic commits.

### Migration map (1543 → 458 lines)

| Step | Commit | Migrated | Destination |
|------|--------|----------|-------------|
| 7a | `d96a503` | vad / asr_partial / drop callbacks | `src/sensus/auditus/auditus_facade.{h,cpp}` (new) |
| 7b | `dbd9f9e` | transcript / asr_log / stats / speaker_match | same facade |
| 7c | `cd3224d` | WS binary PCM ingress (`set_on_binary`) | same facade |
| 7d | `7e1c9ef` | 545-line WS text command router (`set_on_text`) | `src/actus/cmd_test_ws_router.{h,cpp}` (new peer TU) |
| 7e | `628dd69` | 91-line `set_on_connect` hello envelope | `src/actus/cmd_test_ws_hello.{h,cpp}` (new peer TU) |

Interim `d97f1ba` bumped a `char[128]` → `char[256]` to silence a
real `-Wformat-truncation` on the tracker_reg_event fragment in the
facade stats callback.

### Design crystals

**Why the router became a peer Actus TU, not part of auditus_facade.**
The 545-line switch dispatches to AudioPipeline (vad/gain/speaker DB
CRUD), SpeakerTracker, *and* ConscientiStream (enables, sampling,
prompts, text injection). Forcing it into `auditus_facade` would have
dragged Conscientia/Persona headers across the R3 boundary. An Actus
verb is by construction a cross-subsystem integration point, so the
router belongs *beside* `cmd_test_ws.cpp`, not inside any single
subsystem facade. Namespace `deusridet::actus`, not
`deusridet::auditus`.

**Why connect/disconnect stayed out of Auditus (Step 7c scope cut).**
The `set_on_connect` handler broadcasts a 4096-byte consciousness
state envelope (wakefulness, KV cache metrics, persona, per-pipeline
sampling configs) plus a separate prompts envelope. None of that is
Auditus territory — it is a client-greeting surface that belongs to
a future Nexus- or Conscientia-level facade. Keeping it in 7c would
have poisoned the Auditus facade's type boundary. We pulled it out,
added Step 7e as the correct home: a peer Actus helper
`send_consciousness_hello(...)` that takes the minimum set of refs.

**Facade option A (stateless).** `auditus_facade` is a namespace of
free `install_*_callback(...)` functions. No facade class, no locks,
no new threads. AudioPipeline and WsServer continue to own their
lifetimes; the facade only wires callbacks. This preserved the
existing timeline-log ↔ WS ordering contract exactly, because the
callbacks still run on the same worker threads.

### R1 status

- `src/actus/cmd_test_ws.cpp`: **458 lines**, under R1's 500-line cap.
- `src/actus/cmd_test_ws_router.cpp`: 512 lines. Router body is almost
  entirely `if/else if` branches with JSON replies; each branch is
  short; no single function exceeds R1's soft limit.
- `src/sensus/auditus/auditus_facade.cpp`: 465 lines.
- All 13 pre-existing R1 violations in the backlog table remain;
  Step 9 will attack them.

### Byte-identical observable behaviour

Every one of the five migrations preserved exactly:
- Every `snprintf` format string (field names, order, precision).
- Every `send_text` / `send_binary` / `broadcast_*` call site.
- Every stdout log line, every `timeline.append_*` call.
- The atomic identities for `total_frames`, `total_bytes`, `loopback`
  (shared between binary callback and text router via reference).
- The shared_ptr-captured hysteresis state for `multi_speaker_last` /
  `multi_speaker_initialized` (moved into the facade stats installer
  without becoming a file-scope global).

### Verification

Each of 7a/7b/7c/7d/7e: `cmake .. && make -j$(nproc)` → clean build
(only the pre-existing format-truncation warning during 7a/7b, fixed
in `d97f1ba`). Runtime: `HTTP=200` on `/`, `WS=101` on `/ws`.
`test-ws` comes up, serves the WebUI, accepts WS upgrades, and tears
down cleanly on `SIGKILL`.

### Backlog state going forward

- Step 7 ✅ closed.
- Step 8 (Nexus / Memoria / Persona / Orator facades) is unblocked:
  `auditus_facade` is the proven template.
- Step 9 (CUDA/audio R1 split campaign) waits on Step 8.

---

## Step 9 — CUDA/audio R1 split campaign (closed same day)

20 atomic commits `57ecd1a` → `172b264`. Every originally-oversized
file in the Refactor Backlog resolved. Each split independently
verified (`cmake + make` + awaken HTTP=200 WS=101).

### Campaign mechanics

- Seams were picked at philosophical boundaries (subsystem
  responsibility, kernel family, algorithmic stage), not at arbitrary
  line counts.
- Shared CUDA kernels moved to sibling `.cuh` as `static __global__`
  so each peer TU gets a private instantiation (RDC off in this
  build). This keeps launch-latency unchanged and avoids linker
  surgery.
- Subsystem `CMakeLists.txt` files already use `GLOB_RECURSE` across
  `sensus`, `orator`, `machina`, `conscientia`, `communis`, and
  `nexus` — every new peer TU is picked up without edits.

### Summary table (abbrev.)

| # | Commit | Target | Before → After |
|---|--------|--------|-----------------|
| 1 | `57ecd1a` | auditus_facade.cpp | 525 → 130+421 |
| 2 | `0fbf4b1` | awaken_router.cpp | 577 → 437+185 |
| 3 | `db3651f` | ws_server.cpp | 607 → 359+277 |
| 4 | `ec7d74a` | asr_engine.cpp | 611 → 488+149 |
| 5 | `cc5c43b` | spectral_cluster.h | 626 → 66+590 |
| 6 | `5e5407b` | tokenizer.cpp | 665 → 443+268 |
| 7 | `7f7f738` | stream.cpp | 836 → 464+400 |
| 8 | `5a9a9a4` | model.cpp | 982 → 227+426+381 |
| 9 | `5a4f295` | audio_pipeline.h | 1068 → 481+81+365+199 |
| 10 | `d3ab5e8` | asr_ops.cu | 898 → 535+378 |
| 11 | `1fe0aa2` | gptq_gemm_v2.cu | 1092 → 656+461 |
| 12 | `5023a33` | marlin.cu | 1125 → 379+555(.cuh)+238 |
| 13 | `7ccd43a` | frcrn_gpu.cu | 1263 → 787+512 |
| 14 | `856392d` | speaker_vector_store.cu | 1411 → 498+684+276 |
| 15 | `0b3349c` | mossformer2.cu | 1551 → 35+590+518+425(.cuh)+70(.h) |
| 16 | `cf71b11` | layer.cu | 1960 → 552+495+436+555 |
| 17 | `6406777` | gptq.cu | 2036 → 276+419+182+380+346+342+207+26(.cuh) |
| 18 | `9cebc7e` | wavlm_ecapa_encoder.cu | 2091 → 448+416+361+238+743(.cuh) |
| 19 | `132f529` | forward.cu | 2179 → 639+247+339+336+431+320(.cuh) |
| 20 | `172b264` | audio_pipeline.cpp | 2656 → 327+1574+260+558 |

### Residuals → Step 11

Three peer TUs emerged from splits #20 and #5 still above the .cpp
500-line cap, but each contains **exactly one** non-decomposable
method/function:

- `audio_pipeline_process.cpp` (1574) — `AudioPipeline::process_loop`
- `spectral_cluster.cpp` (590) — `spectral_cluster()`
- `speaker_tracker_check.cpp` (558) — `SpeakerTracker::check`

The R1 campaign's primary-school work — breaking monolithic files
apart — is done. The next surgical pass reaches into the function
body.

### Step 11 opened — function-level decomposition

Scope: A3 (spectral_cluster, pure algorithm, cleanest template) →
A2 (speaker_tracker::check) → A1 (process_loop, largest, cross-
subsystem — apply the pattern once proven).

Not the same flavour as Step 9. Step 9 was mechanical: seam, split,
verify. Step 11 is conceptual: identify the **stages** hiding inside
a monolithic body, name them, extract them as private helpers, and
leave the top-level method as a ~80-line dispatcher. Success criterion
is not just line count but inspectability — each stage must be
independently trace-taggable from the WebUI.

