# DEVLOG 2026-05-27

## Phase 3c — First semantic read of the production diarisation path

Run: `runs/20260526T051055_replay_s600/` — first 600s of `tests/test.mp3`,
replay 4×, reclusterer ON (defaults: W=180s, S=30s, K∈[2,6],
link=0.55, ema=0.20, min_segs=12, max_segs=300, dual-encoder 384D).

Read against `tests/fixtures/test_ground_truth.json` directly by the agent
per the system-level "no scripted scoring on semantic judgement" rule
(`workflow.instructions.md` §Semantic Evaluation, commit 40ab28d).
What follows is *pattern → evidence → suspected cause →
candidate intervention*, no macro-F1.

### Pattern 1 — Catastrophic 4 → 2 global collapse at the first reclusterer pass

**Evidence.** The final forward-map after 600s is `{0,1,2,5} → 1000` and
`{3,4} → 1001`. GT has four speakers (朱杰 / 徐子景 / 唐云峰 / 石一). All
the heavy lifting happens at `t=03:24.500` (the first window-end after
the 180s warmup): in a single burst, raw clusters `0`, `1`, `2` are
all relabelled to global `1000`, while `3` is relabelled to `1001`.
Subsequent passes at `04:10.700` and `05:12.400` only consolidate the
same two buckets.

**Suspected cause.** Within-window K-selection appears healthy — the
eval driver's earlier full-corpus logs show "Optimal K=4 / K=5" being
picked per window. The collapse therefore lives in the **inter-window
Hungarian linker**, not in spectral clustering. The first window
(00:00–03:00) sees raw=0 dominantly producing 朱杰's long monologue
(00:22–00:39, ~10s clean), with raw=1 sprinkled across 朱杰 / 徐子景 /
唐云峰 short turns, and raw=3 just starting to emerge as 唐云峰
(01:42 onward). The Hungarian linker, given a 384D centroid still
dominated by 朱杰's evidence, accepts raw=1 + raw=2 + raw=0 all as
"same global" (link threshold 0.55 is too generous when EMA centroids
have not separated yet) and assigns raw=3 a new global. This matches
the working hypothesis already in `2026-05-26.md` Phase 14 ("full_60m
macro_f1 is dominated by inter-window Hungarian linkage stability").
Production confirms it now.

**Candidate intervention.**
- Tighten linker threshold from 0.55 → ~0.65 *during the bootstrap
  phase* (e.g. first N≤2 windows), or make the threshold a function
  of how many segments the EMA centroid has integrated.
- Require linker bidirectional agreement (candidate must be the
  best match from both sides, not just "above 0.55 to one of the
  globals").
- Bootstrap delay: increase `min_segs` from 12 → ~24 or require
  ≥2 windows before the first relabel event ever fires. Let raw IDs
  accumulate richer evidence before any global merge.

### Pattern 2 — The system self-corrects after ~05:18 once 4 globals exist

**Evidence.** Starting at `06:16.800`, raw `7` appears and stays clean
through `07:05` — every overlap there is 徐子景 (`GT=徐子景  pred=7`).
Starting at `07:20.100`, raw `6` appears and dominates 石一's stretch
through `08:43` (with two stray 唐云峰 utterances mixed in at 08:24
and 08:26, when 石一/唐云峰 alternate rapidly). Crucially, raw=6 and
raw=7 are *not* in the forward-map — the reclusterer never tried to
merge them. So once four reasonably-separated globals exist, the
linker preserves identity. The pathology is bootstrap-specific.

**Implication.** This is encouraging — the steady-state behaviour of
the pipeline is correct. The bug is in the first 3–5 minutes when
EMA centroids are immature. A bootstrap-aware policy (delay first
relabel, tighter threshold while centroids are young) should be a
high-leverage fix.

### Pattern 3 — A long monologue flips global mid-stream

**Evidence.** 唐云峰's 03:27–03:58 monologue is rendered as
`pred=1000` (raw=1) for the first half (03:27–03:43) then as
`pred=1001` (raw=3) for the second half (03:47–03:55). Same speaker,
same continuous utterance, two different global IDs. The online linker
emits both raw=1 and raw=3 within one speech event; the reclusterer
then maps raw=1 to 朱杰's global (1000) and raw=3 to its own global
(1001) — splitting one 唐云峰 monologue across what the listener will
read as two different people.

**Suspected cause.** Upstream raw-cluster fragmentation: the online
speaker-ID + segment-linker stack emits multiple raw IDs for one
continuous speaker turn. The reclusterer is the right place to heal
this, but it heals in the wrong direction because of Pattern 1.

**Candidate intervention.** Fixing Pattern 1 likely fixes this too —
if raw=1 had been linked to raw=3 (both唐云峰) instead of to raw=0
(朱杰), the monologue would be coherent. So the linker policy fix is
load-bearing.

### Pattern 4 — Margin-abstain is well-behaved

**Evidence.** ~25 `pred=-1` decisions, almost all on (a) ≤0.5s onset
fragments, (b) overlap regions where two speakers compete, (c) the
"嗯 / 啊" backchannels that VAD did catch but speaker-ID correctly
refused. None of the `pred=-1` are on confident long-form utterances.
The conservative-abstain pathway is doing its job and is not the
source of error.

**Action.** No change needed here.

### Pattern 5 — VAD drops ~12 short utterances entirely

**Evidence.** "no runtime segment overlaps this utterance" appears
12× in the first 600s, all on utterances ≤2s (e.g. 朱杰 "啊？" at
03:03, 唐云峰 "嗯。" at 08:21, 徐子景 "对." at 09:26). These are
backchannels and one-syllable interjections.

**Suspected cause.** SileroVAD / FSMN-VAD min-speech-ms gate is
likely too long, or the 25-ms hangover is too short to keep
isolated yes/no syllables. This is a `sensus` (perception) issue,
not an `orator` issue.

**Action.** Out of scope for Phase 3. File for later VAD tuning if
the dialogue-understanding stack starts caring about backchannels.

## Conclusion for the 3b/3c roadmap

The dominant failure mode is **bootstrap-phase Hungarian linker
over-merging**, not within-window spectral clustering and not the
forward-map mechanics. This finding came from reading the transcript
narratively — every numeric scoring lever I tried in Phases 8–14
either regressed full_60m or moved s1800 by ≤1pp because they were
operating on the wrong subsystem.

Next action (Phase 3b candidate, to be designed and
implemented separately, not in this devlog):
1. Audit the linker's exact decision at `t=03:24.500` — log the
   global-vs-cluster cosine matrix it sees and the assignment.
2. Try `link_threshold=0.65` + `min_segs=24` as a bootstrap policy
   (only for the first 2 reclusterer passes, then back to defaults).
3. Verify on both s1800 and full_60m via the existing eval driver
   *before* wiring into auditus.
4. Run the same replay capture + agent semantic read on the new
   forward-map and check the merge events are now {朱杰-only},
   {唐云峰-only}, etc., not the current cross-speaker buckets.

Quote guiding the work, from Thomas:

> "测试不要用脚本去分析，而是直接读取上下文。"
> "Don't analyse with scripts. Read the context directly."

## Phase 3b.1 — Linker audit (env-gated, eval driver at s600)

Added `DEUSRIDET_RECLUSTERER_DEBUG=1` env-gated audit in
`src/orator/orator_reclusterer.cpp`. At every spectral pass it dumps to
stderr: (a) per-cluster member list (segment_id, raw_id, t_start–t_end),
(b) the full `cluster × global` cosine matrix with Hungarian assignment
bracketed, (c) link_threshold gate per row, (d) every new-global
allocation, (e) every global-merge collapse with sim, (f) the final
`local_to_global` table. Off by default, zero cost when off.

Re-ran the eval driver over the same audio slice that produced the s600
production replay:

```
DEUSRIDET_RECLUSTERER_DEBUG=1 \
  ./build/orator_reclusterer_eval \
  --fixture tests/fixtures/fused_v1.bin --end-sec 600 \
  --W 180 --S 30 --thr 0.55 --ema 0.20
→ macro_f1=0.9046 K_pred=5 K_mapped=4/4 n_scored=198 n_events=299
```

Captured: `runs/20260527_audit_s600/audit.log` (451 lines).

### What the audit shows on the offline pipeline

Pass-by-pass picture of the first 600s of `fused_v1.bin` (same audio
material as the production replay):

| Pass @ now (s) | N | K_spec | G_in | New globals | Behaviour |
|---|---|---|---|---|---|
| 36.5  | 13 |  2 | 0 | 1000, 1001 | first pass — every cluster fresh |
| 70.0  | 26 |  2 | 2 | —          | k=0→1001 sim=0.7292, k=1→1000 sim=0.9380 |
| 101.3 | 39 |  2 | 2 | —          | both match cleanly (0.97, 0.72) |
| 132.4 | 50 |  3 | 2 | **1002**   | new cluster, top-1 sim=0.61 < hung-assigned 1000 → fresh id |
| 163.2 | 60 |  2 | 3 | —          | matches 1000 (0.98) and 1002 (0.98); 1001 unused |
| …     |    |    |   |            | …                                                       |
| 553.4 | 64 |  3 | 5 | —          | matrix top-1s all ≥ 0.92 — labels stabilised |
| 583.7 | 63 |  3 | 5 | —          | top-1s 0.94 / 0.94 / 0.93                              |

Globals 1003 and 1004 are allocated during the t=302.8s–553.4s gap.
By the end of s600 the offline pipeline holds **five persistent
globals**, mapped onto **four GT speakers**, with `macro_f1 = 0.9046`.

### Revised root cause — Phase 3c hypothesis was wrong

Phase 3c had hypothesised a "catastrophic 4→2 Hungarian over-merge" at
t=03:24.500 of the production replay. The audit refutes this:

1. **Spectral never picks K=4 in the first window.** Looking at the
   actual 0-180s GT distribution (朱杰 ≈62s monologue + 唐云峰 ≈10s
   short turns + 徐子景 ≈5s + 石一 ≈3s), the data only contains enough
   material for two stable clusters. K=2 is the correct spectral
   answer for the data at that window. The Hungarian linker is
   innocent — there are simply no four clusters to over-merge.

2. **The 3rd and later globals emerge as soon as the window
   accumulates enough material.** Global 1002 is allocated at
   t≈132s, 1003 at t≈303–553s, 1004 in the same range. The
   reclusterer's "discover new speakers as they accumulate" mechanic
   works fine on the fixture.

3. **The discrepancy is between fixture-driven eval and the live
   pipeline.** Eval at s600 → 5 globals, macro_f1=0.9046. Live
   production replay at s600 → only 2 globals (`{0,1,2,5}→1000,
   {3,4}→1001`). Same audio, same reclusterer code, same defaults
   (W=180s, S=30s, thr=0.55, ema=0.20). The bug must live in **what
   the live pipeline feeds the reclusterer**, not in the reclusterer
   itself.

### Phase 3b — revised next steps

1. **Audit live `push()` input.** Add the same env-gated dump on the
   live audio-pipeline side (`audio_pipeline_process_saas_full.cpp`):
   log every `rseg.push()` with seg_id, raw_id, t_start/t_end, and
   embedding L2-norm. Compare segment count and timestamps against
   the fixture for the same audio slice.
2. **Likely suspects** (in priority order):
   - **Gate `match.speaker_id >= 0` drops segments** the eval fixture
     contains. Online linker's "unknown" turns never reach the
     reclusterer.
   - **Live VAD segmentation differs from the offline run that built
     `fused_v1.bin`** — same VAD code path but driven by different
     buffering / replay timing.
   - **Live `dual_emb` extraction**: 384D fused vector is built once
     per finalised utterance from the live FBank stream; the fixture
     was extracted from a different streaming run.
3. **Do NOT tweak link_threshold / min_segs / bootstrap policy yet.**
   The reclusterer is not the broken subsystem. Tightening its knobs
   would mask the real upstream defect.

### Audit instrumentation footprint

- `src/orator/orator_reclusterer.cpp` +~70 lines, all under `if
  (debug_enabled())`. `debug_enabled()` reads
  `DEUSRIDET_RECLUSTERER_DEBUG` once via a function-local static.
- No new CLI flag, no API change, no `.h` change.
- Zero runtime cost when env var is unset.

## Phase 3b.2 — Live push() audit (H1 & H2 verified, H3 ruled out)

Added env-gated audit `DEUSRIDET_LIVE_PUSH_DEBUG=1` in
[`src/sensus/auditus/audio_pipeline_process_saas_full.cpp`](src/sensus/auditus/audio_pipeline_process_saas_full.cpp)
at the reclusterer push site. Logs every FULL extraction's gate outcome
(ACCEPT seg/raw/t/L2 or REJECT reason). Re-streamed `tests/test.mp3` 600s
@ 4× through `tools/e2e_relabel_smoke.py` (same path as production
replay) and reproduced the `{0,1,2,5}→1000, {3,4}→1001` collapse.

Artefacts: `runs/20260527_live_push_audit_s600/{stderr,smoke}.log`.

### Counts

| Metric | live (600s) | fixture (s600) | ratio |
|---|---:|---:|---:|
| FULL extractions reaching push site | 72 | — | — |
| pushed to reclusterer (ACCEPT) | **34** | 198 | **17%** |
| rejected by `match.speaker_id >= 0` gate | **38** | 0 | **53% drop** |
| ACCEPT L2 norm | 1.000000 | (pre-normalised) | numerically clean |

ACCEPT raw distribution: `{raw=0:4, raw=1:4, raw=2:1, raw=3:10, raw=4:1, raw=5:1, raw=6:8, raw=7:5}`.
REJECT spans the entire 0-600s timeline (not just bootstrap):
`t_end=13.7, 35.3, 62.4, 67.5, 72.5, 99.2, 106.0, 109.3, 115.0, 117.7, 143.2, …, 204.0` etc.

### H1 verified — `match.speaker_id >= 0` gate eats 53% of segments

Every reject carries `sid=-1 sim=0.000 is_new=0`. These are segments where
the online identify path (campp/dual_db identify, recency absorb-guard, or
margin-abstain) chose to abstain. The fbank frames are real speech
(fbank=188..528 frames = 1.9..5.3s of audio). The gate at line ~329 of
`audio_pipeline_process_saas_full.cpp` discards them before the
reclusterer can see the embedding. Reclusterer was designed exactly to
override the online linker's judgement, so gating its input by that same
linker's decision is a structural inversion.

### H2 verified — VAD/segment density is the dominant starvation factor

Even ignoring the gate, the live pipeline produces only 72 FULL
extractions across 600s vs the fixture's 198 GT-cut segments — a 2.75×
density gap. The fixture is built from `cam_embeddings_v1.meta.json`
(GT-aligned turn cuts averaging ~3s/seg); live VAD cuts on silence /
overlap heuristics and averages ~8s/seg. With `min_segments=12` and
W=180s, this means the first reclusterer pass on live sees a window
dominated by 朱杰's ~62s monologue plus a handful of 唐云峰 turns;
徐子景 (~5s GT total in 0-600s) and 石一 (~3s) simply have too few
samples to form a stable cluster, so spectral correctly returns K=2.

Note: H2 is not a "bug" — the fixture is artificially over-segmented at
GT granularity. The live VAD output is the realistic operating point.
The eval's `macro_f1=0.9046` therefore overstates available headroom.

### H3 ruled out — embedding numerics

All ACCEPT entries report `L2=1.000000 dim=384`. The dual embedding
(CAM++ 192 ⊕ WL-ECAPA 192) is L2-normalised before push, exactly as in
the fixture build. No numerical divergence.

### Dominant lever

H1 is the only clean, low-risk fix. Lifting the `match.speaker_id >= 0`
clause restores 38 segments (72 → 72, but reclusterer-visible goes
34 → 72, **+112%**). At 72 segs / 600s the live density approaches the
density at fixture pass t=583s (N=63, K=3, 5 globals stable). Plan
3b.3: A/B with `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1` env flag;
keep abstain-segs in the reclusterer with `tentative_speaker_id=-1`,
emit RelabelEvent old=-1, verify WebUI patcher tolerates it.

H2 is acknowledged as an upstream-VAD design parameter, not a bug.
Tightening live VAD would reduce avg seg duration but at the cost of
purity (more overlap-contaminated cuts), so it's out of scope for this
phase.

H3 is closed.

## Phase 3b.3 — A/B verification of H1 fix (success)

Env-gated A/B at the live push site in
[`src/sensus/auditus/audio_pipeline_process_saas_full.cpp`](src/sensus/auditus/audio_pipeline_process_saas_full.cpp):
`DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1` lifts the
`match.speaker_id >= 0` clause. Abstain segments enter the reclusterer with
`tentative_speaker_id=-1`; the spectral pass alone decides their global ID.

To get a complete forward-map I also widened the smoke event dump from
`relabels[:20]` to `relabels[:]` in
[`tools/e2e_relabel_smoke.py`](tools/e2e_relabel_smoke.py). The earlier
report of "still 2 globals" was a truncation artefact, not the live behaviour.

Artefacts: `runs/20260527_h1fix_s600/{stderr,smoke}.log`.

### Counts vs Phase 3b.2 baseline

| Metric | baseline (no fix) | with H1 fix | delta |
|---|---:|---:|---:|
| FULL extractions reaching push site | 72 | 70 | (live jitter) |
| pushed to reclusterer (ACCEPT) | 34 | **70** | **+106%** |
| rejected by sid_negative gate | 38 | **0** | **−38** |
| `raw=-1` admitted | 0 | **32** | new path |
| total relabel events | 31 | 80 | more activity |
| distinct segs touched | (truncated) | 67 | full visibility |
| **K_pred final** | **2** (forward `{0,1,2,5}→1000, {3,4}→1001`) | **4** (1000:23, 1001:20, 1002:17, 1003:7) | **K_gt=4 matched** |

Reclusterer is doing exactly what it was designed to: ignoring the linker's
abstention verdict, running the rolling spectral pass over a denser, unbiased
input stream, and discovering all four speakers.

### Conclusion

- Phase 3-14 negative experiments measured a reclusterer that was being
  starved at the input — 53% of real speech segments never reached it.
- The "macro_f1=0.7032 on full_60m / 0.7935 on s1800 fixture" baselines
  were measured on the gate-free fixture path; they did not capture the
  live gate-filtering bug.
- The reclusterer's defaults (W=180, S=30, link=0.55, centroid_ema=0.20,
  min_k=2, max_k=6) are correct for this regime. No reclusterer knob
  needs tuning; the bug was upstream and a single env-gated boolean fixes it.
- The flag is left env-gated (`DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1`)
  so production behaviour is unchanged by default. Promotion to
  always-on belongs in a separate change once we have a second
  long-form audio confirming the result.

## Phase 3b — Retrospective audit & forward plan (sign-off)

After 3b.3 landed, the project owner declared three project-wide rules:

1. **Real recording + transcript is the canonical test reference.**
   `tests/test.mp3` + `tests/fixtures/test_ground_truth.json` (556
   utterances, 4 speakers, ~60 min) is the only acceptable behavioural
   ground truth. No discount.
2. **No detached / no half-system tests.** Eval-driver style runs that
   bypass VAD / dual_db / gates may run, but their numbers MUST NOT
   influence default values or ship/no-ship decisions. They are flagged
   `internal-check-only`.
3. **All historical decisions must be re-evaluated under (1)+(2).**

### Retrospective verdict on Phase 2 → Phase 14

Out of 16 orator-touching commits since `ec38202` (Phase 2):

- 3 **infrastructure** commits (wiring, WS plumbing): valid (Phase 4 /
  Phase 7 / Phase 11).
- 13 **performance / tuning** commits used fixture-only evidence: their
  numeric verdicts are no longer trusted. Among them, **Phase 6
  (`window_sec=180` default)** is the only one that changed a live
  default value. It is hereby marked `unverified` and queued for live
  re-test in Layer 2 of the plan below.
- Phase 3b.1 (linker self-audit) + Phase 3b.2 (live push audit) +
  Phase 3b.3 (env-gated H1 fix, K_pred=4 matching K_gt=4) are the
  first observations made under the new rules and are kept.

### Forward plan (Layer 0 → Layer 4)

- **Layer 0** — codify the three rules in `philosophy.instructions.md`,
  `workflow.instructions.md`, `benchmarks.instructions.md`; annotate
  `tools/orator_reclusterer_eval.cpp` and `tests/fixtures/fused_v1.bin`
  as internal-check-only.
- **Layer 1** — one full 60-minute live replay on `test.mp3` with H1
  fix ON, produce agent-readable pairing report; this report becomes
  the trusted baseline against which every future change is compared.
- **Layer 2** — live A/B sweep `window_sec ∈ {120,150,180,240,300}`;
  agent reads 5 reports and selects the best W; commit either confirms
  Phase 6 default or retires it.
- **Layer 3** — at the chosen W, live A/B
  `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED ∈ {0,1}`; if =1 wins on real
  long-form audio, the env gate is removed and =1 becomes always-on.
- **Layer 4** — semantic audit of the baseline report (pattern →
  evidence → suspected cause → candidate intervention); decides whether
  next focus is VAD, dual_db identify, reclusterer, or ASR.

Standing rule going forward: any orator/sensus knob change requires a
live A/B with agent-read pairing report before commit; macro-F1 / fuzzy
matching / any "auto-judged correctness number" is forbidden as
ship-gate evidence.

### Pending sign-off questions and the answer

1. Accept blanket ❌ on Phase 3–14 performance verdicts? **YES.**
2. Execute Layer 0 → Layer 4 in order? **YES.**
3. Pause further orator tuning until Layers 1–4 finish? **YES.**

Owner instruction appended: commit + push before every action; preserve
every step including mistakes.

## Layer 1 attempt 1 — speed=4.0 ring buffer overflow (failed, preserved)

First baseline attempt: full 60min `tests/test.mp3` at `--speed 4.0`
with `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1`, W=180.

Within 3 minutes of wall time the awaken log accumulated **617**
`Ring buffer overflow, dropped 3200 bytes` warnings. Pipeline was
not keeping up at 4x for the longer-duration run (prior 600s 4x
runs succeeded, so the overflow is cumulative, not setup error).

Per benchmark rule "lower the speed until backend keeps up", the
attempt was aborted before pairing completed. Artefacts preserved
locally (gitignored) at:

- `runs/20260527_layer1_attempt1_speed4_overflow/raw_events.jsonl`
- `runs/20260527_layer1_attempt1_speed4_overflow/awaken.log`

Retry: same parameters but `--speed 2.0` (60 min audio → ~30 min wall).

## Layer 1 attempt 2 — speed=2.0, awaken died silently (failed, preserved)

Second baseline attempt: full 60min `tests/test.mp3` at `--speed 2.0`,
W=180, `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1`. Launched awaken with
`./build/deusridet awaken > /tmp/awaken_layer1_v2.log 2>&1 &` (no
`nohup`, no `disown`).

After ~3 min wall the replay client died with
`WebSocketConnectionClosedException: Connection is already closed.` at
`stream_pcm` line 60. `pgrep -af deusridet` then returned empty —
awaken was gone too. The awaken log was clean up to its last line
`07:01:52.145 DIAG rms=0.0155 silero=0.157 speech=0 gain=4.0 spk=-1`
with **no** segfault / OOM / FATAL marker. `dmesg` showed only
PCIe AER *corrected* errors from the NVMe controller (after 07:02:10,
i.e. after both processes died); kernel did not log an OOM kill.
Ring buffer overflows: 0.

**Suspected cause.** The chat shell that backgrounded awaken with `&`
held it as a session child. When tool-orchestration polled with
`pgrep -af deusridet`, the wrapping bash process was already gone, so
awaken received SIGHUP on the way out. No graceful-shutdown log line
was emitted because the signal handler doesn't log to stdout before
returning.

Artefacts preserved locally (gitignored) at
`runs/20260527_layer1_attempt2_speed2_awaken_crashed/{raw_events.jsonl,
runner.log, awaken.log}`.

**Fix.** Retry with `nohup env DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1
./build/deusridet awaken > … 2>&1 & disown` and the same for the replay
client. This is now the recommended Layer 1 launcher.

## Layer 1 attempt 3 — speed=2.0 + nohup, clean baseline (succeeded)

Launcher (canonical for Layer-N baselines from here on):

```
nohup env DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1 \
  ./build/deusridet awaken > /tmp/awaken_layer1_v3.log 2>&1 & disown
nohup python3 tools/replay_to_transcript.py \
  --audio tests/test.mp3 --gt tests/fixtures/test_ground_truth.json \
  --max-sec 3615 --speed 2.0 --drain-sec 60 \
  --out-dir runs/20260527_layer1_baseline_full_w180 \
  > runs/.../runner.log 2>&1 & disown
```

**Wall time.** 3615 s audio @ 2× + 60 s drain ≈ 1807 s stream + 60 s
drain = ~31 min wall. Matches the runner's `[stream] sent 3615.0s src
in 1807.5s wall`.

**Health.** Ring buffer overflows: **0** (cf. attempt 1's 617 at 4×).
Awaken survived the whole run; `[awaken] WS client disconnected (fd=42)`
at end-of-drain, then idle. No segfault, no OOM. raw_events.jsonl =
79 MB; awaken.log = 15 522 lines / 1.26 MB.

**Pipeline counters (from runner.log).**

- `speaker=1105` speaker decisions
- `amend=10` in-place amendments
- `relabel=479` forward-map merges (the reclusterer is doing its job)
- `asr=0` (ASR gated off by default — expected)
- `stats=36150` pipeline_stats messages
- runtime VAD segments: **1483**
- runtime speaker segments paired: **1105**

**Reclusterer trajectory (from `transcript.md` head + relabel_log).**

- First merge burst at `t=01:49.2`: raw clusters {0,1} → 1000,
  raw 2 → 1001.
- By `t=05:55.9`: 1001 → 1002 promotion, raw 6 → 1002.
- Stable canonical IDs that survive to end-of-run: **{1000, 1001,
  1002, 1003, 1004}** — i.e. **K_pred = 5**, against **K_gt = 4**
  (唐云峰 / 徐子景 / 朱杰 / 石一). One extra cluster — consistent with
  H1's expected behaviour (ACCEPT_ABSTAINED=1 admits more FULL
  evidence, which adds resolution but also lets one minor speaker
  split). Last relabel event: `t=59:45.7 7 → 1003 conf=0.973`.

**Anomaly flagged for Layer 1b semantic read.** `forward_map.json` shows
every runtime id 0–7, 1000–1003 mapping to GT-index `1003`, and only
`1004 → 1004`. That is a degenerate collapse — either the pairing
script `pair_runtime_to_gt` is mis-keyed against GT or one canonical
speaker absorbed nearly all utterances. Must inspect transcript.md
top-to-bottom (agent's eyes, not a script) before believing it.

Artefacts preserved locally (gitignored) at
`runs/20260527_layer1_baseline_full_w180/{runner.log, awaken.log,
runtime_segments.json, relabel_log.json, forward_map.json,
transcript.md, raw_events.jsonl, asr_events.json}`.

**Next.** Layer 1b — agent reads `transcript.md` top-to-bottom against
GT, reports *pattern → evidence → cause → intervention*. Layer 2
(window_sec sweep) blocked until Layer 1b interpretation is in.

## Layer 1b — semantic read of the live baseline (CRITICAL FINDING)

Re-reading `runtime_segments.json` with the relabel chains replayed
(each segment's `current_id` walked through its `relabel_chain` to the
final canonical id) reveals that the "K_pred=5" reported in Layer 1
attempt-3 was an artefact of looking only at the IDs that *appeared*
in any relabel event. The IDs that *survive* end-to-end on actual
segments are:

| final canonical id | n segments | total speech time |
|--------------------|-----------:|------------------:|
| `1003`             | **834**    | **1233.0 s**      |
| `-1` (abstain)     | 267        | 604.0 s           |
| `1004`             | 4          | 2.4 s             |

Effective **K_pred = 2** (one dominant cluster + a 2.4-second outlier),
not 5. Against **K_gt = 4** speakers. This is the same catastrophic
collapse that Phase 3c documented at 600 s — it now reproduces on the
full 60-minute run with `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1` ON.

**Time-stratified distribution (10-min buckets):**

```
t=[ 0-10] n=170 1003=143 (-1)=27
t=[10-20] n=176 1003=138 (-1)=34 1004=4
t=[20-30] n=195 1003=158 (-1)=37
t=[30-40] n=176 1003=131 (-1)=45
t=[40-50] n=200 1003=134 (-1)=66
t=[50-60] n=184 1003=129 (-1)=55
t=[60+]   n=4   (-1)=3  1003=1
```

The collapse is **uniform across the entire hour**. There is never a
moment when two non-degenerate speakers coexist. 1003 absorbs everything
from minute 0; the 4 brief 1004 segments in the 10–20-min bucket are a
transient.

### What this overturns

1. **H1's earlier "K_pred 2→4" verdict is contradicted by live evidence.**
   The 4-cluster result was measured against `tests/fixtures/fused_v1.bin`
   under the offline eval driver — exactly the kind of detached test
   that Prime Directive #8 (committed `2060f48`) now classifies as
   `internal-check-only`. The live system under the same flag produces
   K_pred=2, not 4. ACCEPT_ABSTAINED=1 admits more FULL evidence but the
   inter-window Hungarian linker still merges everything into one
   cluster.

2. **Phase 3b.3's "fix" did not fix the live pipeline.** The
   commit `0997e8a` lifted the gate `match.speaker_id >= 0` so FULL
   extractions push regardless of abstain, but the downstream linker
   is the actual bottleneck. Lifting more evidence into a broken
   linker doesn't repair the linker.

3. **The bug is in inter-window linking, not in within-window K
   selection.** Phase 3c already suspected this; the 60-min live run
   confirms it. The two-pass spectral clustering inside each 180 s
   window is healthy (it produces several raw ids that *do* get
   different labels at the start of each window — 0,1,2,3,4,5,6,7
   all appear). But the moment those raw ids hit the Hungarian linker
   and try to align across windows, they collapse onto the same
   canonical id (1003).

### Suspected cause (to verify in Layer 2)

The inter-window linker uses a centroid-based cosine match with a
threshold (`link=0.55`). Across a 60-min conversation with four
similar speakers (all native Mandarin, similar SNR), centroids drift
within a small enough hyper-cone that *every* new window's clusters
land within 0.55 of *every* prior canonical centroid. The linker
picks the highest-similarity prior, and on a 4-speaker conversation
the central canonical (whichever was created first) eats every
subsequent cluster by max-similarity greedy matching.

**Candidate interventions (NOT to be tried yet — Layer 2 sweeps come
first):**

- Raise the link threshold (`link=0.65` or `0.70`) to force new
  canonical ids when the within-window centroid is not unambiguously
  the same speaker. Risk: creates excess clusters that themselves
  need re-merging.
- Replace centroid-cosine with **per-canonical exemplar bank**
  matching (top-k exemplars) — already partially present in the
  dual-encoder DB; the linker should query it the same way the FULL
  decision path does.
- Run the linker on **windowed sub-centroids** (e.g. 30 s sub-windows
  inside the 180 s window) so that a long monologue from speaker A
  doesn't drag the canonical centroid into A's region irreversibly.

### Action items

- **Layer 2 is no longer just a window_sec sweep.** Window size will
  not fix a broken linker. Layer 2 must instead sweep `link` threshold
  (`{0.55, 0.60, 0.65, 0.70, 0.75}`) at fixed W=180 to see whether the
  inter-window match is the proximate cause.
- All earlier verdicts on reclusterer defaults that cite
  `fused_v1.bin` / `fused_v2_dominant.bin` are now **⚠️ unverified**
  under Prime Directive #8 — including Phase 3b.3 H1's "K_pred=4"
  claim. The bilingual Phase 3b.3 entries in
  `docs/{en,zh}/devlog/2026-05-21.md` should carry that flag.
- The pairing script's `forward_map.json` collapse to `1003` is
  *correct* given the data — almost every segment really ends up at
  1003. It is not a script bug; the script is faithfully reporting
  the catastrophic collapse.

## Layer 2 — matrix_v1 link-threshold sweep (full 60min live)

Five cells, fixed W=180s, S=30s, ema=0.20, K=[2,6],
`DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=1`. Per-cell knob:
`DEUSRIDET_RECLUSTERER_LINK_THRESH ∈ {0.55, 0.60, 0.65, 0.70, 0.75}`.
Same audio, same GT, speed=2.0, drain=60s, commit `e407109`, binary
sha256 `951a943…068f`. All five cells: 0 ring buffer overflows,
replay_exit=0, wall_sec ≈ 1874–1879. Artefacts in
`runs/matrix_v1/L0{55,60,65,70,75}_A1/` (gitignored).

| cell    | link | n_seg | abstain % | K_pred_final | top id (segments / share of non-abstain) |
|---------|------|------:|----------:|-------------:|------------------------------------------|
| L055_A1 | 0.55 | 1105  | 24.16     | 2            | 1003: 834 / 99.52% |
| L060_A1 | 0.60 | 1144  | 18.53     | **1**        | 1002: 932 / 100.0% |
| L065_A1 | 0.65 | 1055  | 26.73     | 3            | 1005: 744 / 96.25% |
| L070_A1 | 0.70 | 1129  | 22.41     | 3            | 1002: 833 / 95.09% |
| L075_A1 | 0.75 | 1105  | 24.43     | 3            | 1006: 806 / 96.53% |

K_gt = 4 (`唐云峰`, `徐子景`, `朱杰`, `石一`).

**Finding.** Sweeping `LINK_THRESH` from 0.55 to 0.75 does not change
the qualitative outcome. In every cell, a single canonical id absorbs
≥ 95% of all non-abstain segments. K_pred is never ≥ K_gt; the only
K_pred ≥ 2 cells still have a single dominant id and a 7–36 segment
tail. K_pred is non-monotonic in link threshold (2 → 1 → 3 → 3 → 3),
so there is no "sweet spot" hiding in this 0.05-step grid.

**What this rules out.** The collapse to K=1–3 is not caused by an
overly-permissive link threshold. Raising the threshold by 0.20 (from
0.55 to 0.75) only moves which integer id label wins; it does not let
new canonical ids survive end-to-end on real segments.

**What is left to test.**

1. `DEUSRIDET_RECLUSTERER_ACCEPT_ABSTAINED=0` column (matrix_v1 A0):
   the abstain → forward-map path may be the real cause of collapse.
   Currently 18–27% of segments are `-1`; if those carry the
   speaker-change information, accepting only confident embeddings
   may itself be a centralising force.
2. Window size `W` (currently hard-wired in code, not env-tunable).
   Smaller W = fewer cross-window merges, may preserve more ids.
3. The relabel-chain replay logic itself: 479–500+ relabel events
   per run, and the chain replay shows almost every initial id
   eventually collapses to the same canonical. Worth inspecting the
   chain depth distribution.

Aggregated JSON: `runs/matrix_v1/_aggregate.json` (gitignored).


## Layer 2b — semantic read: accuracy ≈ 0, reclusterer destroys upstream signal

I re-read the matrix_v1 runs as accuracy (GT speaker vs final predicted id),
not as cluster counts. The result is unambiguous and the previous "K_pred"
framing was hiding it.

**Setup.** For each cell, every GT utterance (`speaker`, `t0_start`, `t0_end`)
is overlaid on the runtime segments; each runtime segment's final id is the
end of its `relabel_chain`. For each GT speaker, accumulate overlap-seconds
per predicted id.

**Result — predicted id per GT speaker, all five cells:**

| link | 唐云峰    | 徐子景    | 朱杰      | 石一      |
|------|----------|----------|----------|----------|
| 0.55 | → 1003   | → 1003   | → 1003   | → 1003   |
| 0.60 | → 1002   | → 1002   | → 1002   | → 1002   |
| 0.65 | → 1005   | → 1005   | → 1005   | → 1005   |
| 0.70 | → 1002   | → 1002   | → 1002   | → 1002   |
| 0.75 | → 1006   | → 1006   | 1006/-1  | → 1006   |

Every GT speaker gets the same id in every cell. The reclusterer **does not
distinguish the four speakers**. Accuracy for "which of the 4 people is
speaking?" is ~0 across the entire matrix.

**Where the signal actually is — upstream (raw_id from online speaker_db,
before reclusterer):**

| GT speaker | most-used raw_id (overlap-sec / share of speech)              |
|------------|---------------------------------------------------------------|
| 唐云峰      | raw=3: 197s (18%),  raw=7: 94s,  raw=-1: 211s                 |
| 徐子景      | raw=6: 88s (24%),  raw=-1: 59s,  raw=7: 19s                   |
| 朱杰       | raw=1: 84s (15%),  raw=-1: 123s,  raw=7: 22s,  raw=0: 19s     |
| 石一       | raw=7: 567s (36%),  raw=-1: 212s,  raw=3: 31s                 |

Each of the four GT speakers has a *different* most-used raw_id. The online
speaker_db is weak (raw=7 acts as a generic catch-all and absorbs ~5–10%
from the other three speakers; 13–23% of every speaker's time goes to
raw=-1) but the four-way distinction is *present* in the raw ids.

**Conclusion.** The reclusterer collapses every raw id (0,1,2,3,4,5,6,7)
into one global id, destroying the partial separation that the upstream
already had. Sweeping `LINK_THRESH` from 0.55 to 0.75 only changes *which
number* the single surviving global id has (1003 → 1002 → 1005 → 1002 →
1006); it does not let a second global id survive end-to-end on real
segments.

**Root cause is in `src/orator/orator_reclusterer.cpp::run_pass`**, not in
the env knobs. Specifically: the rolling-window spectral pass produces K
local clusters, the Hungarian linker attaches every one of them to (or
near) a single existing global, and the EMA + global-merge passes finish
the job by funneling all K into one. The link threshold sweep proves this:
even at 0.75 (very strict re-use), every cluster either gets re-used by
the dominant global or is allocated a new id that itself gets merged in.

**Plan revision.**

- Drop Layer 3 (ACCEPT_ABSTAINED 0 vs 1 sweep) — it cannot fix a
  reclusterer that loses every distinction it was given.
- Next: a single live run with `DEUSRIDET_RECLUSTERER_ENABLE=0` to
  measure the baseline accuracy of raw_id alone. Expected: 25–35% time
  per speaker correctly on their dominant raw_id (vs the current ~0).
- After that: turn on `DEUSRIDET_RECLUSTERER_DEBUG=1` on a short slice
  to dump per-window K and cosine matrix, so we can see *which* of the
  three collapse mechanisms (spectral K too low, Hungarian re-use too
  generous, global-merge too aggressive) is firing.

