# 开发日志 — 2026-04-24

## 第 11 步 —— 函数级分解（收官）

第 9 步之后，仍有三个文件超出 R1 `.cpp` 500 行上限，但它们每个都只
包含一个成员函数——文件级已无法再拆。第 11 步伸手进入函数体本身：
编排器留下，每个算法阶段迁入同胞 TU。三个残余至今全部合规，每个阶段
皆可独立打 trace 标签。

### A3 —— `spectral_cluster()`（提交 `c77efde`）

`src/orator/spectral_cluster.cpp` 590 → **85** 编排器 + 636 阶段
+ 119 头文件。纯算法、零副作用——函数级抽取最干净的模板。私契
`namespace deusridet::spectral_detail` 暴露十个自由函数，逐一对应
原单体中的 `// ===== Step N =====` 块。编排器化为 60 行顺序分发。

### A2 —— `SpeakerTracker::check`（提交 `991f543`）

`src/sensus/auditus/speaker_tracker_check.cpp` 558 → **177** 编排器
+ 500 阶段（加上 `speaker_tracking.h` 中 8 条私有成员函数声明）。
阶段需要类成员状态，因此分解落在类里作私有助手，而非自由函数命名
空间——验证了该模板同样适用于有状态代码。

### A1 —— `AudioPipeline::process_loop`（提交 `df93e8b`、`02011b3`、`a34b4a9`）

规模最大、跨子系统最多的函数体（1574 行）。拆为三个原子子提交，
逐个独立构建并完成 awaken 仪式验证：

| # | 提交 | 抽取 | 编排器残余 |
|---|------|------|-----------|
| A1a | `df93e8b` | `process_asr_pipeline_`（392 行）—— 连续 ASR 累积、VAD 驱动切片、说话人切换判决、裁剪与提交 | 1216 |
| A1b | `02011b3` | `process_saas_full_extract_`（456 行）—— 段结束 CAM++ FULL 抽取 + 双编码器融合 + 谱聚类热身 | 801 |
| A1c | `a34b4a9` | `process_saas_during_speech_`（321）+ `process_saas_segment_end_`（190） | **353** |

`process_loop` 体今已成为干净的八阶段顺序编排器：增益/RMS → FRCRN
→ Silero VAD → FSMN VAD → SAAS 三路分支（起始 / 段内 / 段结束）
→ SpeakerTracker → ASR → Mel/VAD/stats。

### A1 经验

- **三提交原子模式可扩展。** 每个子提交都能独立编译并完成 awaken
  验证。A1b 曾撞上 GLOB_RECURSE 缓存陈旧（`undefined reference to
  process_saas_full_extract_`），单次 `cmake ..` 即解，无需回滚 A1a。
- **参数管道是唯一允许的差异。** A1a 将 `std::vector<int16_t>& pcm_buf`
  提升为 `const int16_t* pcm_buf` + int，内部 `pcm_buf.data()` 调用
  相应更新；其他一切皆逐字搬运。
- **提交信息中的 `!` 会触发 bash 历史扩展。** 改用 heredoc 写入
  `/tmp/msg.txt`，再 `git commit -F`。
- **cwd 纪律。** awaken 仪式必须先 `cd /home/rm01/DeusRidet`；在
  `build/` 下执行会以 127 退出，因为 `./build/deusridet` 无法解析。

文档同步：`0647a98` 在 `docs/{en,zh}/architecture/00-overview.md` 中
将第 11 步标记为**已收官**，附前后行数与提交哈希表。

## 第 13 步 —— R1 残余清账（第 11 步衍生回归）

A3 把十个阶段体装入单个 636 行同胞 TU——自身即超出 R1 `.cpp` 500
行上限。A1/A2 给 `audio_pipeline.h` 追加了 28 行叙事式阶段图注释，
将头文件推到 511。第 13 步两处清账。

### 13a —— 拆分 `spectral_cluster_stages.cpp`（提交 `d6872e9`）

636 → 按算法阶段分为三个同胞 TU，函数体逐字搬运：

| 同胞 TU | 行数 | 阶段 | 相位 |
|---------|------|------|------|
| `spectral_cluster_affinity.cpp` | 219 | 0–3 | PCA + 相似度 + 剪枝 + 对称化/孤立修复 —— "每对有多近" |
| `spectral_cluster_embed.cpp` | 228 | 4–6 | Laplacian 特征分解 + 特征间隙 K + K-means++ —— "几个说话人、哪点归哪" |
| `spectral_cluster_postprocess.cpp` | 235 | 7–9 | 时序平滑 + 中心计算 + 合并 —— "簇到底长什么样" |

私契 `namespace deusridet::spectral_detail` 保持不变；编排器
`spectral_cluster.cpp` 无需任何编辑即可通过编译。

### 13b —— 压缩 `audio_pipeline.h` 阶段图注释（提交 `c4293ac`）

511 → 489。28 行叙事块替换为 3 行指向权威
`docs/{en,zh}/architecture/00-overview.md` § 第 11 步节的指针。
声明本身未变。

### 结果

R1 各上限全线清场——`.cpp`/`.h`/`.hpp` 无 500 行以上者，
`.cu`/`.cuh` 无 800 行以上者。`00-overview.md` 中的重构待办只剩
第 12 步（`awaken.cpp` 126 行 LLM + Conscientia bootstrap 块）
作为架构级——而非 R1 级——待办项。
