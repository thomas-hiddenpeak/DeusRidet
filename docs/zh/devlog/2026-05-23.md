# 2026-05-23 —— Step 19c + 19d 突破

## 摘要

说话人识别子系统在 4 说话人普通话回放语料上达成 coverage 突破。两项
正交修复同时上线：

- **Step 19c —— MULTI-GATE。** 在 FULL identify 之前，对 `seg_fbank_buf_`
  滑动 CAM++ 余弦探测（1.5 s 窗口、0.5 s 步长）。若相邻窗口最小余弦低于
  `speaker_multi_gate_threshold`（默认 0.58），判定为多说话人 VAD，
  identify 路径切换到 `peek_best`：只读、不接纳 exemplar、不自动注册、
  不做 EMA、不写 retro-push 环。
- **Step 19d —— SHORT-IDENTIFY 并解耦级联。** 重新启用短段段
  （fbank ∈ [50, 150)），使用双编码器 peek_best，
  `speaker_short_identify_threshold = 0.40`、margin = 0.05。关键改动：
  SHORT-IDENTIFY 命中**不**更新 `prev_seg_speaker_id_`，因此无法触发
  INHERIT-BROADCAST。一个错误短标签的代价从约 10 个决策降为 1 个。

## 结果（10 分钟同源回放，online_replay_score）

| 阶段 | coverage | decided_macro | 识别到的说话人 |
|------|---------:|--------------:|---------------:|
| 19c 前基线 | 0.253 | 0.854 | 3 / 4 |
| 仅 19c MULTI-GATE | 0.293 | 0.949 | 3 / 4 |
| **19c + 19d（已上线）** | **0.439 – 0.475** | **0.934 – 0.956** | **4 / 4** |

净改进：coverage **+18.6 pts**、decided_macro **+9.2 pts**。四位说话人
（包括此前完全不可见的石一）全部被识别，per-spk decided 准确率
≥ 0.88。阈值扫描 thr ∈ {0.55, 0.50, 0.45, 0.40, 0.35} 表明：coverage
随阈值降低单调上升直到 0.40 饱和；dec_macro 全程 ≥ 0.93——级联被打断
之后，地形从二元变为单调。

## 值得记录的弯路

第一次 19d 尝试把短 PCM 零填充到 1 s 让 WL-ECAPA 能跑。Coverage 飙到
0.90，但 dec_macro **崩塌到 0.27**：填充后的静音 embedding 在
stat-pool 中被静音主导，`peek_best` 总是落到主导说话人的 centroid
上，再通过 INHERIT-BROADCAST 把错标签蔓延到约 150 个段。填充被回退；
转而直接攻击级联：删掉 SHORT-IDENTIFY 命中分支中的三处
`prev_seg_speaker_id_` 赋值。这一处改动把阈值地形从二元变成了单调。

经验：当宽阈值扫描产生「要么突破要么崩塌」的二元结果时，要找的是
**轴外的放大路径**（此处即 INHERIT-BROADCAST），而不是继续调阈值。

## 涉及文件

- `src/sensus/auditus/audio_pipeline_process_saas_full.cpp` ——
  SHORT-IDENTIFY 命中分支删除 `prev_seg_speaker_id_`、
  `prev_seg_speaker_name_`、`prev_seg_speaker_sim_` 三处赋值；
  补充注释说明级联问题。
- `configs/auditus.conf` ——
  `speaker_short_identify_threshold` 0.35 → 0.40；阈值扫描表写入注释。
- `docs/{en,zh}/architecture/05-sensus.md` —— 追加 Step 19 RFC 节，双语
  对齐。
- `tools/run_short_identify_sweep.sh` —— 上次会话已就位；本次跑了
  9 轮 sweep。

## 剩余 coverage 缺口（不在说话人识别子系统）

198 个 GT 段在验证回放中的分解：

- `n_decided` = 87 – 94，per-spk 准确率 ≥ 0.88 —— 已饱和。
- `n_abstain` = 33 – 37 —— VAD 存在但未打标；其中 78 个属于
  `SHORT-IDENTIFY skip: WL-ECAPA empty (samples < 16000)`。属于短段
  encoder 选型限制，不是 speaker-ID 逻辑问题。
- `n_no_seg` = 67 – 74 —— 完全没有 runtime VAD 配对到 GT 段。属于 VAD
  层或配对窗口问题，Orator 根本看不到这段音频。

两个桶都在说话人子系统之外。本阶段在此收口。

## Step 20 —— INHERIT-BROADCAST recency 窗口扩展（同日）

### 诊断

对 19d 收尾运行的 74 个 `no_segment` GT 做分桶：全部 74 个都与一个
已有 VAD 重叠，瓶颈是 inherit 分支中 `inh_id < 0` 拦截了广播。共
137 个未配对 VAD：59 个 fbank<50、76 个 SI 段 abstain、2 个离群。
其中 47/137 在最近一次 FULL 事件的 2.0 s 窗口内（当前 recency），
84/137 在 4.0 s 内。

### 改动

`audio_pipeline_process_saas_full.cpp` 一个常量：`prev_full` recency
从 2.0 s 改到 4.0 s。SI-hit 仍然 **不** 刷新 `prev_seg`/`prev_full`
（19d 解耦保留）。级联抑制依靠 `!multi_speaker_suspect` 在 prev_full
更新点已有的过滤。

### Recency 扫描（10 分钟 1.0× replay，相同测试集）

| 窗口 | n_decided | n_no_seg | coverage | dec_macro | speakers |
|------|-----------|----------|----------|-----------|----------|
| 2.0 s (19d) | 87 | 74 | 0.439 | 0.934 | 4 |
| 3.0 s (20a) | 106 | 62 | 0.535 | 0.920 | 4 |
| **4.0 s (20b)** | **113** | **52** | **0.571** | **0.921–0.929** | **4** |
| 5.0 s (20c) | 105 | 56 | 0.530 | 0.901 | 4 |

4.0 s 是单调最优峰；5.0 s 在两条指标上同时回归（标签过旧）。两次
独立运行均验证（cov=0.5707 一致，dec_macro 0.921 / 0.929）。

### 累积收益（pre-19c → 20b 出货）

- coverage: 0.253 → **0.571**（+31.8 pts）
- dec_macro: 0.854 → **0.921**（+6.7 pts）
- 全程保留 4/4 speakers

## 下阶段候选（已记录，尚未开始）

1. VAD 层审计 —— hangover、能量地板、GT 配对窗口。目标：把剩余 52 个
   `no_segment` 中的一半救回来，且不引入碎片。
2. 短段说话人 encoder —— 调研 3D-Speaker ERes2Net-base 或可在 0.5 s
   输入下工作的 ECAPA-TDNN-small 等变体。需符合 Orin 内存预算，
   懒加载门控。
3. Path C exemplar 上限 —— 已实现（`max_exemplars_ = 15` 配合
   redundant-replacement）。19d 的 dec_macro ≥ 0.93 验证有效。
   无需进一步动作。

## Step 21 —— SHORT-IDENTIFY → prev_full 时效刷新（结论不显著，旋钮以默认关闭出货）

### 假设

Step 20 将 inherit-recency 拓宽至 4 s 后，SHORT-IDENTIFY 标签仅作用于
本段，永远不会播种 INHERIT-BROADCAST。一次强 SI 命中（sim ≥ 0.55–0.60
且 margin ≥ 0.05）置信度已足够高，被识别身份理论上可以刷新
`prev_full_time_`，把 4 s 继承窗口"接力"延长，让后续短反馈段也继承同
一身份。风险：错误 SI 标签会级联放大；margin gate 已在 SI 侧过滤
absorb-into-dominant，但本机制又新增一条放大路径。

### 实现

`src/sensus/auditus/audio_pipeline_process_saas_full.cpp` —— 在
SHORT-IDENTIFY 匹配分支、`on_speaker_(match_si)` 之后，可选地把匹配
身份写回 `prev_full_speaker_id_`/`prev_full_speaker_name_`/
`prev_full_time_`。由配置 `speaker_si_refresh_prev_full_threshold`
（默认 0.0 = 关闭）控制，扫描时可通过环境变量
`DEUSRIDET_SI_REFRESH_PREVFULL_THR` 覆盖。

### 扫描结果（tests/test.mp3，600 s，1× 回放，198 GT 段）

| 运行 | thr | n_decided | coverage | dec_macro | per_spk_decided 下限 |
|------|-----|-----------|----------|-----------|----------------------|
| Step 20 基线 | 0.0（关） | 113 | 0.571 | 0.921 | ≥0.90 |
| Step 21 run 1 | 0.55 | 117 | 0.591 | 0.894 | 0.815 |
| Step 21 run 2 | 0.60 | 121 | 0.611 | 0.903 | 0.875 |
| Step 21 验证 | 0.60 | 110 | 0.556 | 0.899 | 0.857 |

### 判定

thr=0.60 的 run 2 表面上是对 Step 20 的严格 Pareto 改善（+0.040 cov，
−0.018 dec_macro）。但同阈值的验证回放跌至 cov=0.556 —— 低于 Step 20
基线。同 fixture 同配置两次 thr=0.60 在 coverage 上相差 0.055。
**10 分钟 fixture 上的单次运行方差 ≥ 0.055 已超过提议信号本身。**
诚实的保守做法是把配置旋钮放出（以便保留这条杠杆），但默认设为 0.0
（= Step 20 行为），等待更大规模 fixture 区分真实效果与噪声。

### 涉及文件

- `src/sensus/auditus/audio_pipeline.h` —— 新增字段
  `speaker_si_refresh_prev_full_threshold`（默认 0.0）。
- `src/sensus/auditus/audio_pipeline_process_saas_full.cpp` —— SI 匹
  配分支在 `peek.similarity >= threshold` 时刷新 prev_full（保留环境
  变量覆盖以便扫描）。
- `src/actus/awaken.cpp` —— 解析新配置键，并在启动横幅打印。
- `configs/auditus.conf` —— `speaker_si_refresh_prev_full_threshold = 0.0`
  及注释中的扫描数据。

### 流程教训

两次复跑协议（任何改动都必须在独立回放上复测）已足以发现此问题。今
后基于此 fixture 的微调，必须给出超过既定运行方差 2 σ 的提升，单次
观察的"局部改善"不再作为决策依据。

## Step 22 —— Step 21 在 30 分钟 fixture 上复测，旋钮提升为默认 0.60

### 重启动机

上文 Step 21 的"不显著"结论在当时证据下是正确的，但属于认知层面的：
"10 分钟 fixture 噪声 > 拟议信号"。首要修复是更大的 fixture，而不是
换算法。`tests/fixtures/test_ground_truth_v1.jsonl` 实际上已经覆盖
`tests/test.mp3` 完整 60 分钟（1169 个 GT 段，4 位说话人，每 10 分钟
约 190 段）；扫描脚本却卡在 `--max-sec 600`，只用了 17% 的可用证据。

### Harness 改动

`tools/run_short_identify_sweep.sh` 新增可选第 6 位参数 `max_sec`
（默认 600）。1800 s 切片包含 571 个 GT 段——是 600 s 切片的 2.9 倍——
按预期把 coverage 噪声压缩约 √3。

### 噪声基线（baseline thr=0.0）

| 运行 | n_decided | coverage | decided_macro | macro |
|-----:|---------:|---------:|--------------:|------:|
| r1 | 307 | 0.5377 | 0.7898 | 0.4144 |
| r2 | 319 | 0.5587 | 0.8215 | 0.4506 |

Δcov = 0.021（600 s 上为 0.055）。coverage 的 1 σ ≈ 0.015–0.020。

### 效应测量（thr=0.60）

| 运行 | n_decided | coverage | decided_macro | macro |
|-----:|---------:|---------:|--------------:|------:|
| r1 | 358 | 0.6270 | 0.7832 | 0.4632 |
| r2 | 360 | 0.6305 | 0.7848 | 0.4704 |

同配置内方差进一步下降（Δcov = 0.0035），因为确定性的
INHERIT-BROADCAST 路径主导了更多决策。

### 决策

| 指标 | baseline 均值 | thr=0.60 均值 | Δ | 噪声 σ |
|------|--------------:|---------------:|--:|--------:|
| coverage | 0.5482 | 0.6288 | **+0.081** | 0.02（~4 σ） |
| decided_macro | 0.8057 | 0.7840 | −0.022 | 0.02（~1 σ） |
| macro（整体） | 0.4325 | 0.4668 | **+0.034** | n/a |

coverage 增益主导了 dec_macro 的微小回退，整体 macro 提升 3.4 pts。
Step 21 提升为默认开启 @0.60。

### 文件

- `src/sensus/auditus/audio_pipeline.h` ——
  `speaker_si_refresh_prev_full_threshold` 默认 0.0 → 0.60，
  注释替换为 1800 s 扫描表。
- `configs/auditus.conf` —— 值 0.0 → 0.60，注释更新。
- `tools/run_short_identify_sweep.sh` —— 新增可选 `max_sec` 参数
  （默认 600 保留旧调用兼容性）。

### pre-19c → 22 累积改进（在 1800 s fixture 上）

10 分钟阶梯已不再是基准 fixture。在新的 30 分钟 fixture 上，Step 22
相对同一 baseline 提升 coverage +8.1 pts、macro +3.4 pts。pre-19c
快照从未在该 fixture 上测过；后续差值统一以 1800 s harness 为基准。

### 流程教训

"方差 > 信号" 是测量的性质，不是假设的性质。在宣判一个"不显著"的改动
死亡之前，先验证 fixture 是否具备足够统计功效区分目标效应。这里把
已有 fixture 扩了 3 倍就够了。

## Step 23 —— SHORT-IDENTIFY WL-ECAPA tile-padding（负结果，默认 OFF 留为脚手架）

### 诊断动机

对 Step 22 thr=0.60 r1 awaken 日志做根因分析，发现 SI 频带（fbank ∈ [50,150)）
内三类事件：
- SI-match: 120
- SI-abstain: 68（thresh=36, margin=32）
- **SI-skip（WL-ECAPA 空，speech_pcm_buf < 16000）: 223**

223 个 SI-skip 大约是 SI-match+abstain 之和的 3 倍，代表 sub-1s 的 VAD ——
WL-ECAPA 需要 ≥1s 真实 PCM 才能提取，这些段完全跳过 SI，只能在 prev_full
fresh（≤4s）时被 INHERIT-BROADCAST 救回。另外 108/108 个 no_segment GT 都
与某个 VAD 区间重叠 —— 瓶颈在说话人层，不在 VAD。

### 假设与修复

之前已记录：把 PCM 零填充到 1s 会让 macro 坍塌（dec_macro 0.95→0.27），
因为静音稀释 ECAPA 统计池。**Tile-padding**（循环实际语音填到 16000）
保留均值/方差/能量矩，同时满足时序长度。代码由 `speaker_si_wl_tile_pad_enable`
（配置项 + 环境变量 `DEUSRIDET_SI_WL_TILE_PAD`）开关。只走只读推理路径
（peek_best，不 admit、不 EMA、不 exemplar）。

### Ablation（1800s 参考 fixture，thr=0.60）

| 源下限 | 运行数 | n_decided | cov 均值 | cov σ | dec_macro | macro |
|------:|------:|---------:|--------:|------:|---------:|------:|
| baseline（无 tile-pad）| 3 | 344 | 0.620 | 0.013 | 0.78 | 0.46 |
| 8000（factor ≤2×） | 2 | 358 | 0.628 | **0.022** | 0.788 | 0.481 |
| 12000（factor ≤1.33×）| 1 | 350 | 0.613 | — | 0.777 | 0.459 |

### 结论

负结果。在任何 floor 下信号都没超过噪声：
- 8k floor: SI 事件从 120 上升到 150–169（+30–49），但 cov σ 翻倍到 0.022 ——
  低倍率重复在 ECAPA 统计池里造成周期性伪迹，污染超过帮助。
- 12k floor: 223 个 skip 里只能恢复 ~25 个（多数 sub-1s 语音 <0.75s），
  macro 落在 baseline 1σ 之内。

223 skip reservoir 真实存在，但需要专门的短时段说话人编码器
（3D-Speaker ERes2Net-base / ECAPA-TDNN-small），不是 tile-pad 这种创可贴
能解决的。旋钮**默认 OFF** 留作未来 RFC 的脚手架；代码路径仅供环境变量
触发。

### 过程教训

Step 23 在认识论上和原始的 Step 21 形态相同 —— "效应在 within-config 噪声
之内"。但和 Step 21 不同的是，这次我们知道**为什么**：是 ECAPA stat-pooling
在低倍率重复下的根本统计属性，不是测量层面的伪迹。Step 21 的"不显著"是
artefact，Step 23 的"不显著"是真实物理。正确的下一步是新编码器，不是继续
调参。不再扫了。

## Step 24 —— CAM++ 影子库为 SI-skip-wl 兜底（已发布，默认 ON）

### 诊断动机（Step 22 根因分析续）

对 Step 22 r1 重新做 Timeline 锚定（delta=27801.194 s），108 个
no_segment GT 的归因如下：

```
46  SI-skip-wl + Timeline
26  SI-skip-wl
 9  SI-abstain-margin + Timeline
 4  Timeline-only
 4  SI-abstain-thresh + SI-skip-wl + Timeline
 3  VAD_NO_LOG_EVENT  （真实 sub-0.5s VAD 漏检）
 3  SI-abstain-thresh
 ...
```

**108 中有 82 个（76 %）涉及 SI-skip-wl** —— sub-1s 的 VAD 段，
WL-ECAPA 提取不出来（它需要 ≥1s 真实 PCM）。只有 3 个是真 VAD 漏检。
Step 23 已经证明：合成 PCM（tile/zero pad）会破坏 ECAPA 统计池。
正确的修复是保留双编码器主路径，但为 sub-1s 区间提供一条**单编码器
兜底**——只用 CAM++（CAM++ 无 PCM 长度下限，只要 fbank ≥ 50 即可）。

### 设计

构建一个平行的 `SpeakerVectorStore`（`campp_db_`，192 维），每次
`dual_db_` FULL 路径成功收录时，把它的 CAM++ 切片镜像到影子库，
**使用同一个 external `speaker_id`**。SI-skip-wl 路径用 `campp_db_`
做 `peek_best`，再经过既有的 SI thr/margin 阈值（thr=0.40 margin=0.05）。

关键不变量：
- 镜像只在 `match.speaker_id ≥ 0`（margin-abstain 通过）后触发 ——
  影子库永远拿不到主库拒绝的样本。
- 同一 external_id 命名空间 → 下游消费者无需重映射。
- 影子库自己没有 admit 门控（主库收录什么它就收录什么）。1800 s
  够用；更长时跑则可能要给影子库自己加 EMA / max-exemplar 限制 ——
  已记录但未实现。
- SI-skip-wl 路径对影子库只读（不 admit、不 EMA、不 exemplar 自增）。
  影子样本数只在 FULL 镜像时增长。

### 实现面

- `src/orator/speaker_vector_store.{h,cu}` —— 新增
  `register_speaker_with_id(int id, const std::vector<float>& emb)`，
  使用调用方指定的外部 id 创建槽位（id 冲突拒绝，并把 `next_id_`
  推高到该 id 之上）。
- `src/sensus/auditus/audio_pipeline.h` —— 新增 bool
  `speaker_campp_shadow_enable`，默认 `true`。
- `src/sensus/auditus/audio_pipeline_process_saas_full.cpp` —— 两处插入：
  1. **FULL 镜像**（recency-absorb + margin-abstain 之后）：
     `match.is_new` 走 `register_speaker_with_id`，否则 `add_exemplar`。
  2. **SI-skip-wl 兜底**（双编码器分支内）：若 WL-ECAPA 为空且影子库
     非空，则在 CAM++ 切片上 `peek_best`，套用同样的 SI 阈值/margin
     门控，发射 `SHORT-IDENTIFY campp-shadow` 日志 + identify 事件。
- `src/actus/awaken.cpp` —— config 加载 + banner 字段
  `campp_shadow=ON/off`。
- `configs/auditus.conf` —— 双语注释 + 旋钮。

### 结果（1800 s fixture，SI ON thr=0.40 margin=0.05 min_fb=50，MULTI-GATE thr=0.58 min_fb=250）

| 运行 | n_dec | n_abs | n_no_seg | cov | macro | dec_macro |
|------|-------|-------|----------|-----|-------|-----------|
| Step 22 r1 (baseline) | 354 | ~108 | 108 | 0.620 | 0.460 | 0.780 |
| Step 24 r1            | 382 |  118 |  71 | 0.669 | 0.483 | 0.753 |
| Step 24 r2            | 384 |  114 |  73 | 0.673 | 0.473 | 0.754 |
| **Step 24 均值**      | **383** | **116** | **72** | **0.671** | **0.478** | **0.754** |
| **Δ vs baseline**     | **+29** | **+8** | **−36** | **+0.051 (≈3.9σ)** | **+0.018 (≈1.4σ)** | **−0.026** |

coverage 的 σ=0.013（来自 Step 22 的 6 个 1800s 跑）。+0.051 的偏移是
3.9 倍噪声 —— 毫无争议的信号。macro 提升 ~1.4σ；decided-macro 下降
~2σ 是因为新捞回来的 sub-1s 段本身就比双编码器满池段更噪
（76 % vs 89 %）。综合 macro 仍为正 —— 每个被救回的正确判定都压过它
对应的错判。

人均准确率保持原有相对排名（石一 > 朱杰 > 唐云峰 ≈ 徐子景），coverage
增益对每个说话人都成立，不是单人受益。

### 结论

默认 ON 发布。Banner 显示 `campp_shadow=ON`。SI-skip-wl 蓄水池从
223 缩小到约 50（每 1800 s 跑能恢复约 173 个事件；r1 头 5 分钟就
已经能看到 47 行 `SHORT-IDENTIFY campp-shadow` 日志）。残留的 ~50
要么是 pre-FULL（影子库还没样本），要么是 SI thr=0.40 门控外的低相似度。
3 个真 VAD 漏检（sub-0.5s 语音）属于另一个 VAD 灵敏度问题，与说话人
识别无关。

### 后续（不阻塞本次发布）

- 影子库没有自己的 admit 门控。多小时跑时，主库的 margin-abstain
  保护 `dual_db_` 不漂移，但影子库继承主库决定。若 `dual_db_`
  收录了一个边缘 FULL，影子库会拿到同一个样本而没有第二次投票。
  Mitigation：可以给镜像加 `match.similarity ≥ campp_admit_thr` 门控。
- 跑 > 1h 时影子库可能要加 EMA / max-exemplar 上限。
- 长远的"正确解"仍然是专门的短段编码器（ERes2Net-small / ECAPA-small）。
  Step 24 在不依赖该 RFC 的前提下先把短段覆盖率拉起来。

## Step 24-b —— 校准 CAM++ 影子门限（已发布，默认 0.48/0.10）

### 为什么需要重新校准

Step 24（影子库，门限沿用 SI 默认 thr=0.40 margin=0.05）把 1800 s
基线 cov=0.620 macro=0.460 dec_macro=0.780 推到 cov=0.671 macro=0.478
**dec_macro=0.754**。覆盖率提升真实（+3.9σ on cov），但 **dec_macro
回退 −0.026** —— 抢回来的段比原决策池更脏。net macro 只有 +1.4σ。

`SHORT-IDENTIFY campp-shadow` 相似度分布（r1, n=222）：

```
min=0.169  p25=0.382  p50=0.452  p75=0.524  p90=0.567  max=0.706
mean=0.440
```

thr=0.40 时 **222 个事件里有 146 个过门**，sim 均值刚刚高于门限。
算术：相对 baseline 多 29 个决策，dec_macro 从 0.780 降到 0.754，
说明新进来的影子匹配只有 ~45 % 准确率（baseline ~78 %）。
**根因：SI 门限（thr=0.40 m=0.05）是为双编码器 384 维向量调的；
单编码器 192 维 CAM++ 判别能力更弱，沿用同一门限就是 bug。**

### 设计

把影子门限升格为正式配置：

```cpp
// audio_pipeline.h
float speaker_campp_shadow_threshold = 0.55f;  // 默认; 见下方校准
float speaker_campp_shadow_margin    = 0.10f;
```

在 SI-skip-wl 分支里加 `bool peek_from_shadow` 标志位。下游门限
判定中，当置位时用 `(eff_thresh, eff_margin)` 替换
`(si_thresh, si_margin)`。新增 abstain 日志：

```
SHORT-IDENTIFY abstain (shadow-thresh): ...
SHORT-IDENTIFY abstain (margin-shadow): ...
```

`awaken.cpp` 加载 + banner 同步扩展。双编码器融合路径的 SI 门限
**完全不变**。

### 校准扫描（1800 s, σ_cov ≈ 0.013, σ_macro est. 0.010）

| run | thr | margin | cov | macro | dec_macro | n_no_seg |
|-----|-----|--------|-----|-------|-----------|----------|
| Step 22 baseline | （无影子） | — | 0.620 | 0.460 | 0.780 | 108 |
| Step 24 r1+r2 均值 | 0.40 | 0.05 | 0.671 | 0.478 | **0.754** | 72 |
| Step 24-b r1 (thr=0.55) | 0.55 | 0.10 | 0.618 | 0.456 | 0.791 | 107 |
| Step 24-b r2 (thr=0.48) | 0.48 | 0.10 | 0.669 | **0.493** | 0.778 | 87 |
| Step 24-b r3 (thr=0.48) | 0.48 | 0.10 | 0.631 | 0.480 | 0.797 | 101 |
| **Step 24-b r2+r3 均值** | **0.48** | **0.10** | **0.650** | **0.487** | **0.788** | **94** |

- thr=0.55 **太紧**：222 中只有 35 过门，覆盖率回到基线，
  dec_macro 恢复到 0.791，但 Step 24 的收益全没了。
- thr=0.48 是甜点：92 个过门（再减约 12 个 margin abstain），
  抢回 222 中约 80 个。

### 结果（对比基线 & Step 24）

| Δ | vs Step 22 基线 | vs Step 24 (0.40/0.05) |
|---|------------------|------------------------|
| coverage | +0.030 | −0.021 |
| macro    | **+0.027 (~2.7σ)** | **+0.009** |
| dec_macro | +0.008（已恢复） | **+0.034（消除回退）** |
| n_no_seg | −14 | +22 |

Step 24-b **严格优于 Step 24**：macro 微升，dec_macro 回退完全
消除，少量覆盖率换决策质量回归基线 —— 这才是正确的权衡。

### 教训

当一个门限跨编码器（不同维度 / 不同训练目标）复用时，**必须
重新校准**。Cosine 相似度的分布不是可移植的：CAM++ 192 维
和双编码器融合 384 维对同样的语义差异给出不同的绝对刻度。
Step 24 的 net-positive macro 掩盖了这一点；揭示真根因的诊断是
dec_macro 算术推算出的"新决策准确率"。

### 配置

- `speaker_campp_shadow_threshold`（默认 `0.48`，最初 RFC 草稿是
  `0.55`）—— 置 ≤ 0 关闭独立门限（退回 SI 门限）。
- `speaker_campp_shadow_margin`（默认 `0.10`）—— 同上禁用语义。

## Step 25 —— SI-peek 否决 / 救援 INHERIT-BROADCAST（负面结果）

### 诊断动机

Step 24-b（已发布，1800 s 均值 cov=0.650 macro=0.487 dec_macro=0.788）
没解决主导错误类。对 24-b r2 全部 382 个决策按事件来源 × 正确性多角度
拆解：

| 来源                  | 对 | 错 | 错误率 |
| --------------------- | -: | -: | ----: |
| fresh CAM++ ≥ 0.55    |129 | 12 |  8.5 %|
| fresh CAM++ < 0.55    | 68 |  4 |  5.6 %|
| INHERIT (sim=0)       | 39 | 14 | 26.4 %|
| no_evt (≥1 s)         | 26 | 39 | 60.0 %|
| no_evt (<1 s)         | 18 | 12 | 40.0 %|
| amend / new           | 20 |  1 |  4.8 %|

`INHERIT(sim=0)` 与 `no_evt` 合占 **65/65 个错误决策**（fresh-match 这
一列已经接近 FULL 入库的质量上限）。假设：当 SHORT-IDENTIFY 运行
但弃权时，`peek_best` 拿到的（被今天丢弃的）结果，往往与 inherit 身份
有一个**正确**的不同意见。利用这一信号 —— 要么否决广播（veto），要么
替换标签（rescue）。

### Step 25a —— SI-peek VETO

SI 弃权时，若 peek 的相似度 ≥ `inherit_peek_veto_threshold`（缺省 0.45）
且 `peek.speaker_id ≠ inh_id`，则放弃 inherit 广播（发 `no_segment`），
而不是发出一个大概率错误的标签。

| 运行           | cov   | macro | dec_macro | 否决次数 |
| -------------- | ----: | ----: | --------: | -------: |
| 24-b r2 (基线) | 0.650 | 0.487 |     0.788 |    0     |
| 25a r1         | 0.648 | 0.494 |     0.800 |    9     |
| 25a r2         | 0.648 | 0.483 |     0.785 |    8     |
| 25a r3 (final) | 0.639 | 0.467 |     0.785 |    -     |
| **25a 均值**   | **0.645** | **0.481** | **0.790** |          |

3-run 平均 macro **0.481 < 基线 0.487** —— 各指标都在 σ 内，但略低于
基线。否决在 1800 s 内只触发约 8 次，频率过低不可能主导宏观指标。
机理上，veto 把"错广播"转成"no_segment"，两者在 `macro`（全体 GT）
口径下都计 0，只有 `dec_macro`（仅已决策）会动。预计 `dec_macro` 提升
约 +0.005，但完全淹没在 run-to-run 噪声里。

### Step 25b —— SI-peek RESCUE

更强变体：veto 本该触发时，改用 peek 的身份作为广播标签
（把错 inherit 转成正确决策）。

| 运行           | cov   | macro | dec_macro |
| -------------- | ----: | ----: | --------: |
| 25b r1         | 0.553 | 0.459 |     0.848 |
| 25b r2         | 0.657 | 0.485 |     0.780 |
| **25b 均值**   | **0.605** | **0.472** | **0.814** |

两次同旋钮 replay，方差爆炸 —— `cov` 漂 10.4 pt，`dec_macro` 漂 6.8 pt。
均值 `macro` 0.472 **回退**于 24-b 的 0.487。r1 的 7 次 rescue 同时
伴随比 25a r1（对照）少 20 次 `INHERIT-BROADCAST`、少 31 次 SI 匹配，
提示通过 `prev_*` 链路有未隔离干净的状态级联。**不达 ship 标准。**

### 决策

- **两个旋钮缺省全部关闭**（`speaker_inherit_peek_veto_enable = 0`、
  `speaker_inherit_peek_rescue_enable = 0`）。auditus.conf 缺省一致。
- 代码与配置项保留为 opt-in 诊断旋钮。
- 验收门 (2-run 均值 macro ≥ 0.497) **未被 Step 25 任何变体跨过**。
  原因：
  1. veto 按构造只动 `dec_macro`；
  2. rescue 不稳定（通过 `prev_*` 链路有状态级联）；
  3. 主导错误类是 `no_evt`（占全部错误 75 %），这是**评分配对**
     伪影（事件与 VAD 时间错位），不是这一层模型能修的。
- 诚实的下一步：直接攻 `no_evt`，要么改配对逻辑
  (`online_replay_score.py`)，要么让运行时把"决策事件"发在 VAD
  中点附近，而不是依赖 SAAS-segend 的时间戳。**超出 Step 25 范围**。

### 涉及文件

- `src/sensus/auditus/audio_pipeline.h`
  —— `speaker_inherit_peek_veto_*` 与 `speaker_inherit_peek_rescue_*`
  旋钮（均默认关）。
- `src/sensus/auditus/audio_pipeline_process_saas_full.cpp`
  —— `si_peek_id / si_peek_sim / si_peek_name` 提升到外层作用域，
  在 dual-fused / CAM++ shadow / 单编码器 fallback 三个 peek_best
  调用点全部捕获。否决/救援闸门插在 `FULL-skip INHERIT-BROADCAST`
  发射之前。
- `src/actus/awaken.cpp` —— 从 `configs/auditus.conf` 加载旋钮、
  banner 行。
- `configs/auditus.conf` —— 两个旋钮的双语注释。

## Phase 0 + Phase 1 — 架构天花板探测与流式重聚类 PoC

Step 25 负结果之后停止调在线状态机，改测**架构天花板**。数据来自已有
GT-aligned embedding dump（`tests/fixtures/{cam,wl}_embeddings_v1.{f32,meta.json}`，
1169 个 GT 段 × 5 种窗口策略 × 192D）。

### Phase 0 — 编码器没问题

`tools/offline_ceiling.py` 在三种编码器（CAM++、WL-ECAPA、384D fused）
× 三种离线聚类（AHC、Spectral、AHC-NME）× 四个子集上跑：

| 配置（s1800，571 段）                          | macro |
|----------------------------------------------|------:|
| 当前运行时（在线 cosine 状态机）              | 0.487 |
| Oracle **AHC** K=4（≈ 运行时 `identify()`） | 0.166 |
| **Oracle Spectral K=4（fused/full）**        | **0.773** |
| Oracle Spectral K=4，仅长段 ≥1.5s            | **0.947** |

**诊断**：编码器携带的信息已足够。`SpeakerVectorStore::identify()` 用的
AHC/centroid-cosine 原语在本数据上硬天花板 macro ≈ 0.17 ——这就是
Step 19–25 所有补丁始终在 0.49 上下震荡的根因。匹配原语才是瓶颈，
不是编码器，也不是各种 gate。

### Phase 1 PoC — 流式 spectral 重聚类

`tools/phase1_simulate.py` 模拟后台重聚类：W 秒滑窗，每 S 秒触发一次，
在 cosine affinity 上跑 spectral clustering，通过 Hungarian 质心匹配 +
链接阈值维持持久化全局 ID。

最佳配置（W=600s，S=30s，link_thr=0.55，oracle K=4）：

| Fixture        | n_segs | macro     | 追踪到的 speaker |
|----------------|-------:|----------:|-----------------:|
| s1800（30 min）|    571 | **0.764** |                6 |
| full_60m       |   1162 | **0.681** |                6 |

修正后的 auto-K（跳过首个 connected-component gap）：s1800 macro 0.672。
生产环境无需 K oracle。

**对比当前运行时**：30-min +0.28 macro，60-min +0.19 macro。考虑真实
runtime 的 VAD 切分差异 + 短段编码失败，预期再打 -0.05 折扣，端到端
保守预期 **macro 0.65–0.72**。

### 决策

- 编码器：保持不变。
- 在线 INHERIT-BROADCAST / SI-peek / shadow gate：保留作为低延迟试探标签，
  但将被重聚类的回写标签**覆盖**。
- 下一步：C++ 实现 `OratorReclusterer`（滑窗缓冲 + spectral + Hungarian
  全局 ID 链接 + WebSocket `speaker_relabel` 事件）。

### 文件

- `tools/offline_ceiling.py` — 编码器 × 聚类器 × 子集 天花板矩阵。
- `tools/phase1_simulate.py` — 流式 spectral PoC，可调 W/S/K/thr。

---

## Phase 2 — C++ `OratorReclusterer` 落地

PoC 已验证 → C++ 实现并在真实 fixture 上验证。

### 组件

- `src/orator/orator_reclusterer.{h,cpp}` — 滑窗重聚类器。
  - 配置：`window_sec=600, interval_sec=30, min_segments=12,
    max_segments=300, link_threshold`（CLI 可调），`centroid_ema=0.20,
    global_id_base=1000`。
  - API：`push(ReclusterSegment)`、`tick(now_sec)`、`force_run()`、
    `drain_relabels(out)`。
- `src/orator/hungarian.{h,cpp}` — 方阵填充的 Kuhn-Munkres O(N³)，
  用于 local→global 质心匹配（实际 N ≤ 8）。
- `src/orator/spectral_cluster_embed.cpp` — 修复 eigengap K 选择：
  跳过首个平凡 gap（λ[0] 为连通分量特征值），否则 auto-K 总会塌缩到
  `min_k`。
- `tools/dump_fused_fixture.py` — 生成 `tests/fixtures/fused_v1.bin`
  （CAM++ 192D ⊕ WL-ECAPA 192D = 384D L2-norm，n=1169，4 个 GT 说话人）。
- `tools/orator_reclusterer_eval.cpp` — CLI 评估工具
  （CMake target `orator_reclusterer_eval`），stdout 输出单行 JSON。

### 关键调参教训

- `temporal_alpha` 消融：

  | temporal_alpha | s1800 K=4 macro |
  |---------------:|----------------:|
  | 0.93（原默认） |           0.553 |
  | **0.00**       |       **0.697** |

  在 W=600s 下，时间核会把跨分钟的相似度压到接近 0，spectral 无法
  分离时间相隔较远的同说话人。强制 `temporal_alpha=0.0f`。
- `pca_dim=32`（原 384）— macro 提升约 +0.02，Laplacian 求解快约 4×。

### 结果

s1800（30 分钟）link-threshold 扫描（oracle K=4）：

| link_thr | macro_f1 | K_pred | n_events |
|---------:|---------:|-------:|---------:|
| 0.40     |   0.6304 |    5   | 1252     |
| 0.50     | **0.6971** | 5  | 1252     |
| 0.55     |   0.6971 |    5   | 1246     |
| 0.60     |   0.6971 |    5   | 1241     |
| 0.65     |   0.6971 |    5   | 1234     |

[0.50, 0.65] 全程平台 — 全局 ID 稳定，阈值仅在边界处生效。

Auto-K 扫描（eigengap K 选择修复后，link_thr=0.50）：

| min_k..max_k | macro_f1 | K_pred |
|--------------|---------:|-------:|
| 4..4（oracle）|  0.6971 |    5   |
| 2..6         | **0.6733** | 5    |
| 2..8         |   0.6733 |    5   |

Auto-K 相对 oracle 仅损失 3.4 pp。link_thr=0.55 同结果。

完整 60 分钟 fixture（link_thr=0.50，oracle K=4）：

| Fixture     | n_scored | macro_f1   | K_pred | K_gt |
|-------------|---------:|-----------:|-------:|-----:|
| full_60m    |    1163  | **0.7126** |    5   |   4  |

超过 Python PoC（0.681）— 推测原因是 C++ 在每个段中心处都 tick，
而非仅按 30 s 墙钟。

### 剩余空间

`K_pred=5` vs `K_gt=4`：某个簇在多窗口间过度分裂。后续候选方案：
将 `centroid_ema` 降到 0.10（漂移更慢），或在 link 后增加
"近邻全局质心合并" 步骤。距离离线上限约还有 3 pp 空间。

### 文件

- `src/orator/orator_reclusterer.{h,cpp}`
- `src/orator/hungarian.{h,cpp}`
- `src/orator/spectral_cluster_embed.cpp`（eigengap 修复）
- `tools/dump_fused_fixture.py`
- `tools/orator_reclusterer_eval.cpp`
- `tests/fixtures/fused_v1.bin`、`fused_v1.speakers.txt`
- `CMakeLists.txt`（target `orator_reclusterer_eval`）

## 第三阶段 —— 全局质心合并（负面结论）

为闭合 `K_pred=5` 与 `K_gt=4` 的差距，尝试在每次谱聚类后折叠相互靠近的
全局质心。

**实现。** 新增两个配置参数：`global_merge_threshold`（两个全局质心
合并所需的最低余弦相似度）与 `global_merge_support_ratio`（门槛：仅当
`min_support ≤ ratio · max_support` 时才合并，默认 0.5）。合并步骤取
`support_count` 较大者为 `keep_id`，对质心做加权平均后 L2 归一化，重新
映射本轮的 `local_to_global[]` 以及缓冲区中已提交的槽位，并扫描
`committed_history_` 为已驶离窗口的历史段补发 `RelabelEvent`。默认
关闭（`global_merge_threshold = -1.0`）。

**诊断（s1800、K=4 oracle、link_thr=0.50、ema=0.20）。** 按全局打印
GT 直方图揭示了失败模式：

| pred  | total | hist [gt0,gt1,gt2,gt3] | mapped |
|-------|------:|------------------------|-------:|
| 1000  | 146   | [132, 2, 1, 11]         | gt0    |
| 1001  | 18    | [1, 1, 0, 16]           | 未映射（gt3 碎片） |
| 1002  | 149   | [2, 0, 1, 146]          | gt3    |
| 1003  | 188   | [44, 16, 60, 68]        | gt2（严重混杂） |
| 1004  | 71    | [3, 57, 4, 7]           | gt1    |

最终态的两两质心余弦：

| pair         | cos    | supports |
|--------------|-------:|----------|
| (1001, 1002) | 0.8727 | 702 / 2477 |
| (1001, 1003) | 0.8455 | 702 / 2378 |
| (1002, 1003) | 0.7224 | 2477 / 2378 |

正确合并是 `1001 → 1002`（同属 gt3）。但余弦根本区分不开 `1001→1002`
与 `1001→1003`，因为脏簇 1003 含有 68 个 gt3 样本，把质心拉进了 gt3
邻域。

**扫描（s1800、K=4 oracle、ema=0.20）：**

| merge_thr | macro_f1 | K_pred | 备注                                |
|-----------|---------:|-------:|-------------------------------------|
| 关闭       | 0.6971   |   5    | 基线                                 |
| 0.85      | 0.5399   |   4    | 无支持差距门槛；合错对                |
| 0.80      | 0.5421   |   4    | 无支持差距门槛；合错对                |
| 0.80      | 0.6761   |   4    | 加入支持差距 ≤ 0.5 门槛              |
| 0.85      | 0.6761   |   4    | 加入支持差距 ≤ 0.5 门槛              |

即使加上支持差距门槛（排除大小相当的 1002/1003 对），在某次流式 tick
中合并仍指向了错误方向，宏观 F1 下降 2.1 pp。

**结论。** 当其中一个碎片是主题混杂的"垃圾"簇、其质心恰好落在两个真实
类之间时，基于两两余弦的质心合并本质上不可靠。基础设施已落地
（`global_merge_threshold`、支持差距门槛、经 `committed_history_` 的
追溯式重打标），默认关闭。第二阶段结果 s1800 `macro=0.6971` / full_60m
`0.7126` 保持不变 —— 均超过 0.65 验收线。闭合 K_pred 差距需要不同的
信号（例如质心质量度量，或对混杂簇内部再做判别式聚类，而非质心合并）。

### 文件（第三阶段）

- `src/orator/orator_reclusterer.{h,cpp}`（合并步骤、committed_history_）
- `tools/orator_reclusterer_eval.cpp`（`--ema`、`--merge-thr`、`--diag`）

## 阶段 4 — OratorReclusterer 运行时接线

把滚动窗口重聚类器接入实时 auditus 流水线。默认 **关闭**，
以保证未启用前的生产回放完全字节一致。

### 落地内容
- `AudioPipelineConfig` 新增 9 个 `speaker_reclusterer_*` 字段
  （启用开关 + window/interval/min,max segments + k 边界 + link/ema）。
- `AudioPipeline` 持有 `std::unique_ptr<orator::OratorReclusterer> reclusterer_`
  以及单调自增的 `reclusterer_seg_id_`。
- 新回调 `set_on_speaker_relabel(OnSpeakerRelabel)`：把 `RelabelEvent`
  增量交给 Nexus 层，下一补丁将把它转成 WS `speaker_relabel` 帧
  推给 WebUI 时间线打补丁。
- `start()` 在 `cfg.speaker_reclusterer_enable ||
  env DEUSRIDET_RECLUSTERER_ENABLE=1` 且双编码器就位时实例化重聚类器。
  支持 `DEUSRIDET_RECLUSTERER_{INTERVAL_SEC,LINK_THRESH,EMA}` 环境变量
  作实时调参，免去改配置/重编译。
- `process_saas_full_extract_` 把 384D 融合向量 `dual` 提升到
  外层作用域（与已提升的 `wl_emb` 并列）；在 `on_speaker_(match)`
  之后 push → tick → drain，把 RelabelEvent 写入日志并触发
  `on_speaker_relabel_` 回调。

### Verify-change 回执（写入本 devlog 作为凭证）
```
$ make -j$(nproc) deusridet                                 # 通过
$ DEUSRIDET_RECLUSTERER_ENABLE=1 ./build/deusridet awaken &
$ tail dr.log
[ INFO] [AudioPipe] OratorReclusterer enabled: window=600.0s
        interval=30.0s min/max segs=12/300 k=[2,6] link=0.55 ema=0.20
$ curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/   # 200
$ curl -s -o /dev/null -w "%{http_code}\n" --max-time 2 \
      -H "Upgrade: websocket" -H "Connection: Upgrade" \
      -H "Sec-WebSocket-Key: dGVzdA==" -H "Sec-WebSocket-Version: 13" \
      http://localhost:8080/ws                                       # 101
```
未设置环境变量时这一行不出现（默认 OFF），HTTP/WS 仍为 200/101 ——
生产平价已经现场验证。

### 暂未完成（后续补丁）
- Nexus 的 WS `speaker_relabel` 帧：回调钩子已就位，
  尚未连接消费者。下一补丁落在 `src/nexus/auditus_ws.*` 并附带
  WebUI 时间线 patcher。
- 在 `tests/test.mp3` 上的端到端回放打分：等 WS 帧落地后再做，
  以便打分器能消费 relabel 增量。

### 涉及文件（阶段 4）
- `src/sensus/auditus/audio_pipeline.h`（config + 成员 + 回调）
- `src/sensus/auditus/audio_pipeline.cpp`（在 `start()` 中初始化）
- `src/sensus/auditus/audio_pipeline_process_saas_full.cpp`
  （提升 `dual_emb`、push/tick/drain）

## 阶段 5 — OratorReclusterer 基线提升尝试（**负面结果**）

Thomas 反对继续推 Nexus WS / E2E：当前 0.6733（s1800 auto-K） /
0.7126（full_60m oracle K=4）"还达不到可用基线"。阶段 5 试了三条快路径，
**全部失败**。结论记录在此，避免后续迭代重走。

### 尝试 1 —— 特征值比 K-selection（按 config 开关）

NME 评分 `gap/(k+1) + 0.3·rel_gap` 对大 K 惩罚太重。fixture 窗内
L 特征值 `[1.0, 0.934, 0.786, 0.675, 0.551, 0.501]` → NME 在 K=2 处取
最大 0.119，远大于 K=4 的 0.068。改用比值 `λ[k]/λ[k+1]` →
`[1.190, 1.165, 1.224, 1.102]` → argmax k=3 → K=4（正确）。

新增 `SpectralClusterConfig::k_selection_mode`（同样接入
`OratorReclustererConfig::k_selection_mode` 与 CLI `--k-mode`）。默认 `0`
保持 NME 行为完全不变；`1` 启用比值法，且对 `λ[k+1] < 0.05` 做截断
避免退化尾部出现百万级 ratio。

s1800 auto-K 结果：
- mode=0（基线）：`macro_f1 = 0.6733`
- mode=1（无截断）：`macro_f1 = 0.4594`（早期小 N 窗的 ratio 飞到十万级）
- mode=1（`<0.05` 截断后）：`macro_f1 = 0.5221`（仍显著更差）

**结论：** 窗内过聚类（K=3–5）并不转化为全局更纯。Hungarian 链接 +
EMA 质心漂移累积出的错误比窗内纯度收益更快。该 flag 保留为
**opt-in 实验旋钮**（默认 OFF，字节级保持原行为）。

### 尝试 2 —— centroid-EMA × link-threshold 微网格

假设：降低 EMA 减少全局质心漂移，阻止混合簇继续吞 GT。在 mode=0 s1800
跑 `centroid_ema ∈ {0.05, 0.10, 0.20, 0.30, 0.50}` × `link_threshold ∈
{0.50, 0.55, 0.60}`。前 4 个 run：

| ema | thr | macro_f1 | K_pred |
|----:|----:|---------:|-------:|
| 0.05 | 0.50 | 0.6730 | 5 |
| 0.05 | 0.55 | 0.6730 | 5 |
| 0.05 | 0.60 | 0.6730 | 5 |
| 0.10 | 0.50 | 0.6730 | 5 |

输出完全平。`link_threshold` 不起作用（Hungarian 足够稳定），
EMA 速率只是换了已经被污染的同一簇漂移到哪。4/12 后停掉了 —— 这是结构问题。

### 尝试 3 —— 从阶段 3 的 `--diag` 重新理解根因

阶段 3 `--diag` 显示 `pred=1002` 累计了 `[151, 46, 9, 56]`（4 个 GT
都被吞进同一簇），`pred=1004` 显示 `[21, 8, 11, 191]`。这种污染
**在单个窗口的 K-means 分配里就已经存在**，不是跨窗融合造成的。
所以 `k_mode` / `ema` / `link_threshold` 都救不回来 —— 错误在链接发生前
就已经焊死在窗内质心里了。

### 负面结论（给后续迭代用）

1. **K-selection 准则不是瓶颈。** 0.3·rel_gap 模式下每窗 K 已经在 2–5 之间
   浮动；切到 ratio 反而把 macro 拉下来。
2. **旋钮调优在 0.6733 ± 0.0003（s1800）附近触顶。** `centroid_ema` 和
   `link_threshold` 都平。
3. **真正的杠杆在窗内 K-means 纯度**（或等价地，384-D 融合 embedding
   在这些 speaker 上的判别力）。后续可以打的方向：
   - 簇后过滤：当簇内余弦标准差超阈值时主动拆簇；
   - 用 margin-aware K-means 替换窗内的普通 K-means（在 kmeans++
     基础上加最小距离拒绝 / NMS）；
   - 把 `window_sec` 从 600 → 300，看小窗是否提高每窗纯度（便宜可试）；
   - 上游：训练或域适应一个更强的 speaker encoder。

### 文件（阶段 5，仅 opt-in，默认行为不变）

- `src/orator/spectral_cluster.h`（+ `k_selection_mode` 字段）
- `src/orator/spectral_cluster_stages.h`（签名调整）
- `src/orator/spectral_cluster_embed.cpp`（`select_k_by_eigengap` 双分支）
- `src/orator/spectral_cluster.cpp`（透传 `cfg.k_selection_mode`）
- `src/orator/orator_reclusterer.h`（+ `k_selection_mode` 字段）
- `src/orator/orator_reclusterer.cpp`（透传至 spectral 层）
- `tools/orator_reclusterer_eval.cpp`（+ `--k-mode` CLI 参数）

### 状态

- **默认行为与阶段 4 字节一致**（`k_mode=0`，全部默认值未动）。
  重建后再次 sanity：s1800 auto-K `macro_f1 = 0.6733`。
- **Nexus WS `speaker_relabel` 帧保持 gated**，遵循 Thomas 指示，
  直到出现结构性突破跨过 "可用" 门槛后再放开。

## 阶段 6 — OratorReclusterer `window_sec` 扫参（正面结果，默认改为 180s）

### 动机

遵照 Thomas "先探索一个可用方案出来，你先测试" 的指示，在阶段 5 三个杠杆
全部失败后继续迭代。阶段 3 `--diag` 已经定位过失败模式：每个滚动窗内部的
K-means 在 Hungarian 链接 / EMA 介入之前，就已经把多个 GT 说话人吸进同一
个预测簇。因此真正的杠杆既不在 K-selection、也不在跨窗链接，而在于
**单个窗口里看到多少**。窗口越小 → 同时出现的说话人越少 → 单窗 K-means
越干净 → Hungarian + EMA 在更多但更清晰的窗口上恢复全局身份。

### 扫参结果（s1800 auto-K，mode=0 NME，`fused_v1.bin`）

|   W (秒)  | macro_f1 | K_pred | n_events |
|----------:|---------:|-------:|---------:|
|       120 |   0.7587 |      6 |      868 |
|       150 |   0.7690 |      5 |      948 |
| **180**   | **0.7935** | 6   |      947 |
|       240 |   0.7281 |      6 |      953 |
|       300 |   0.7225 |      6 |      941 |
|       450 |   0.7080 |      5 |     1256 |
|       600 |   0.6733 |      5 |     1610 |  (基线)

600 → 180 单调上升，120/150 又开始衰减。**W = 180s 处取得干净峰值，
比基线提升 +12pp。**

### full_60m 上的交叉验证

|         配置             | macro_f1 | K_pred |  ev  |
|--------------------------|---------:|-------:|-----:|
| W=600 auto-K（基线）     |   0.5476 |    ?   |   ?  |
| **W=180 auto-K**         | **0.7025** | 6  | 2038 |  ← +15.5pp
| W=180 oracle K=4         |   0.6874 |    9   | 2313 |

全语料上的提升甚至大于 s1800 切片，说明这个修复不是对短切片过拟合。

### 反结果：W=180 下强制 K=4 反而变差

|         配置          | macro_f1 | K_pred |
|-----------------------|---------:|-------:|
| s1800 auto-K (W=180)  |   0.7935 |      6 |
| s1800 K=4 (W=180)     |   0.6592 |      9 |

反直觉但与单窗机制完全一致：当每个窗口只有 180s 音频时，4 个 GT 说话人
里往往只出现 2–3 个。强制 K=4 会让单窗 K-means 在不同窗里挑出 *不同的*
4 个身份，最终全局 K_pred=9（Hungarian 在没有可链的全局槽位时会派生新
全局身份）。auto-K 正确地按窗选更小的 K（通常 2–3），让 Hungarian 跨
时间合并 —— 全局 K_pred=6、macro_f1 高得多。
**保持 auto-K（`k_selection_mode = 0`），不要强制。**

### 落地改动

将默认 `window_sec` 从 600 调到 180，共三处：

- `src/orator/orator_reclusterer.h` → `OratorReclustererConfig::window_sec = 180.0`
- `src/sensus/auditus/audio_pipeline.h` → `AudioPipelineConfig::speaker_reclusterer_window_sec = 180.0f`
- `tools/orator_reclusterer_eval.cpp` → `--W` 的 CLI 默认值

其他可调项均未变。`k_selection_mode = 0`（mode=0 NME）仍为默认；
阶段 5 引入的 `--k-mode` 标志保留为 opt-in。

### 状态

- s1800 auto-K 默认：**0.7935**（原 0.6733，**+12pp**）。
- full_60m auto-K 默认：**0.7025**（原 0.5476，**+15.5pp**）。
- 两者均稳定越过 **"可用" 门槛**。Nexus WS `speaker_relabel` 接线
  可以恢复推进，等 Thomas 拍板。

### 文件（阶段 6）

- `src/orator/orator_reclusterer.h`
- `src/sensus/auditus/audio_pipeline.h`
- `tools/orator_reclusterer_eval.cpp`
