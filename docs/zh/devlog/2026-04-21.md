# DEVLOG — 2026-04-21

## 第 7 步收官：Auditus 外观 + Actus WS 路由

### 背景

`src/actus/cmd_test_ws.cpp` 是遗留最久的 R1 违规文件——1543 行把
AudioPipeline、WsServer、Orator 说话人库、Persona、Memoria
（CacheManager）、ConscientiStream 全部串在一个连续意识测试台里。
本日五次原子提交让第 7 步彻底落地。

### 迁移映射（1543 → 458 行）

| 步 | 提交 | 迁移内容 | 目的地 |
|----|------|---------|--------|
| 7a | `d96a503` | vad / asr_partial / drop 回调 | `src/sensus/auditus/auditus_facade.{h,cpp}`（新建）|
| 7b | `dbd9f9e` | transcript / asr_log / stats / speaker_match | 同一外观 |
| 7c | `cd3224d` | WS 二进制 PCM 入口（`set_on_binary`）| 同一外观 |
| 7d | `7e1c9ef` | 545 行 WS 文本命令路由器（`set_on_text`）| `src/actus/cmd_test_ws_router.{h,cpp}`（新建平级 TU）|
| 7e | `628dd69` | 91 行 `set_on_connect` hello 封装 | `src/actus/cmd_test_ws_hello.{h,cpp}`（新建平级 TU）|

中途的 `d97f1ba` 把 `char[128]` 扩到 `char[256]`，消除了外观 stats
回调里 tracker_reg_event 片段触发的真实 `-Wformat-truncation`。

### 设计结晶

**路由器为何成为 Actus 平级 TU，而非并入 auditus_facade。**
545 行 switch 同时调度 AudioPipeline（vad/gain/说话人库 CRUD）、
SpeakerTracker、ConscientiStream（启用、采样、prompt、文本注入）。
硬塞进 `auditus_facade` 必然把 Conscientia/Persona 头文件拖过 R3
边界。Actus 动词的本质就是跨子系统的集成点，路由器因此应当与
`cmd_test_ws.cpp` 平级共处，而不进入任何单子系统外观。命名空间为
`deusridet::actus`，不是 `deusridet::auditus`。

**为何 connect/disconnect 留在 Auditus 之外（7c 范围收窄）。**
`set_on_connect` 广播的是 4096 字节的意识状态封装（清醒度、KV 缓存
度量、persona、各管线采样配置）加上单独的 prompts 封装，与 Auditus
无关——这是未来 Nexus 或 Conscientia 层外观的客户端问候面。若塞进
7c 会污染 Auditus 外观的类型边界。我们主动剥离，并追加 7e 作为
正确归属：平级 Actus 助手 `send_consciousness_hello(...)`，只接受
最小必要引用集。

**外观方案 A（无状态）。** `auditus_facade` 是一组自由函数
`install_*_callback(...)` 所在的命名空间，没有外观类、没有锁、没有
新线程。AudioPipeline 与 WsServer 的生命周期仍归调用方所有，外观
只连接回调。这严格保留了既有的 timeline 日志 ↔ WS 顺序契约，因为
回调仍跑在相同的工作线程上。

### R1 状态

- `src/actus/cmd_test_ws.cpp`：**458 行**，落回 R1 500 行硬限以内。
- `src/actus/cmd_test_ws_router.cpp`：512 行。路由器主体几乎全为
  `if / else if` 分支加 JSON 回复；每个分支都短小；没有单一函数
  超过 R1 的软限。
- `src/sensus/auditus/auditus_facade.cpp`：465 行。
- 积压表中原有的 13 处 R1 违规维持不变，交由第 9 步处理。

### 字节对齐的可观测行为

五次迁移全部逐字节保留：
- 每一条 `snprintf` 格式串（字段名、顺序、精度）。
- 每一处 `send_text` / `send_binary` / `broadcast_*` 调用点。
- 每一行 stdout 日志、每一次 `timeline.append_*`。
- `total_frames`、`total_bytes`、`loopback` 的原子身份（二进制回调
  与文本路由器通过引用共享同一个对象）。
- `multi_speaker_last` / `multi_speaker_initialized` 的 shared_ptr
  迟滞状态（搬进外观 stats 安装器，未沦为文件作用域全局）。

### 验证

7a/7b/7c/7d/7e 每步：`cmake .. && make -j$(nproc)` 干净（只有 7a/7b
期间既有的 format-truncation 告警，`d97f1ba` 修复）。运行态：
`/` 返回 `HTTP=200`，`/ws` 返回 `WS=101`。`test-ws` 正常启动、上报
WebUI、接受 WS 升级，收到 `SIGKILL` 可干净退出。

### 后续积压

- 第 7 步 ✅ 关闭。
- 第 8 步（Nexus / Memoria / Persona / Orator 外观）已解锁：
  `auditus_facade` 是已验证的模板。
- 第 9 步（CUDA/音频 R1 拆分大行动）排在第 8 步之后。

---

## 第 9 步 —— CUDA/音频 R1 拆分大行动（同日关闭）

20 个原子提交 `57ecd1a` → `172b264`。Refactor Backlog 中所有原本
超限的文件全部解决。每次拆分独立验证（`cmake + make` + 唤醒仪式
HTTP=200 WS=101）。

### 行动要领

- 拆缝按哲学边界选取（子系统职责、核函数家族、算法阶段），
  而非按任意行数。
- 共享 CUDA 核函数下沉到平级 `.cuh` 并声明为 `static __global__`，
  这样每个平级 TU 拿到独立实例化（本构建 RDC 关闭）。启动延迟不变，
  也无需链接器手术。
- 各子系统 `CMakeLists.txt` 在 `sensus` / `orator` / `machina` /
  `conscientia` / `communis` / `nexus` 下都使用 `GLOB_RECURSE`，
  新增的平级 TU 自动纳入编译。

### 汇总表（简版）

| # | 提交 | 目标 | 前 → 后 |
|---|------|------|--------|
| 1 | `57ecd1a` | auditus_facade.cpp | 525 → 130+421 |
| 2 | `0fbf4b1` | awaken_router.cpp | 577 → 437+185 |
| 3 | `db3651f` | ws_server.cpp | 607 → 359+277 |
| 4 | `ec7d74a` | asr_engine.cpp | 611 → 488+149 |
| 5 | `cc5c43b` | spectral_cluster.h | 626 → 66+590 |
| 6 | `5e5407b` | tokenizer.cpp | 665 → 443+268 |
| 7 | `7f7f738` | stream.cpp | 836 → 464+400 |
| 8 | `5a9a9a4` | model.cpp | 982 → 227+426+381 |
| 9 | `5a4f295` | audio_pipeline.h | 1068 → 481+81+365+199 |
| 10 | `d3ab5e8` | asr_ops.cu | 898 → 535+378 |
| 11 | `1fe0aa2` | gptq_gemm_v2.cu | 1092 → 656+461 |
| 12 | `5023a33` | marlin.cu | 1125 → 379+555(.cuh)+238 |
| 13 | `7ccd43a` | frcrn_gpu.cu | 1263 → 787+512 |
| 14 | `856392d` | speaker_vector_store.cu | 1411 → 498+684+276 |
| 15 | `0b3349c` | mossformer2.cu | 1551 → 35+590+518+425(.cuh)+70(.h) |
| 16 | `cf71b11` | layer.cu | 1960 → 552+495+436+555 |
| 17 | `6406777` | gptq.cu | 2036 → 276+419+182+380+346+342+207+26(.cuh) |
| 18 | `9cebc7e` | wavlm_ecapa_encoder.cu | 2091 → 448+416+361+238+743(.cuh) |
| 19 | `132f529` | forward.cu | 2179 → 639+247+339+336+431+320(.cuh) |
| 20 | `172b264` | audio_pipeline.cpp | 2656 → 327+1574+260+558 |

### 遗留 → 第 11 步

从 #20 与 #5 衍生出三个仍超 .cpp 500 行上限的平级 TU，每个只含
**恰好一个**不可再分的方法/函数：

- `audio_pipeline_process.cpp`（1574）—— `AudioPipeline::process_loop`
- `spectral_cluster.cpp`（590）—— `spectral_cluster()`
- `speaker_tracker_check.cpp`（558）—— `SpeakerTracker::check`

R1 大行动的"小学阶段"——把单体文件拆开——到此完成。下一轮精细
手术要伸进函数体内部。

### 第 11 步开启 —— 函数级分解

路线：A3（spectral_cluster，纯算法，最干净的模板）→ A2
（SpeakerTracker::check）→ A1（process_loop，最大、最跨子系统
——前两者把套路走熟再动它）。

第 11 步与第 9 步不是同一个味道。第 9 步是机械式的：找缝、拆、
验证。第 11 步是概念式的：识别单体函数体内部隐藏的**阶段**，
命名、抽成私有 helper，让顶层方法退化为约 80 行的调度器。
成功准则不止行数，还要**可观测**：每个阶段都能在 WebUI 上独立打
trace 标签。

